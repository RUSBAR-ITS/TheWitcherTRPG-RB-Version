# issue-00206 — Экспорт добычи выбирает тип папки ActiveEffect вместо Actor

<a id="implementation"></a>
## Исправление — 2026-09-17 / 14.3.1.00029

Пользователь согласовал совместное исправление **00206, 00208 и 00218** и отложил игровые тесты до своей команды после перезапуска Foundry. Карточка перенесена в open; закрытие не согласовано. Исторические сведения и ограничения ниже относятся к прежним срезам.

`getOrCreateFolder` в WitcherMonsterSheet явно использует тип `Actor`. Поиск учитывает имя и тип: одноимённая папка ActiveEffect не выбирается; подходящая Actor-папка используется повторно.

Статические/изолированные проверки: Повторное использование Actor-папки при наличии одноимённой ActiveEffect; создание Actor-папки при отсутствии подходящей; передача её ID в создание loot Actor. Всего для группы прошли 19 проверок Node; использованы полные текущие ES-модули в VM, их тела не заменены. Foundry API, диалог, Roll и серверные записи — явно заданные фасады. Это не проверка мира, HTML-валидации браузера или сохранения БД. Также прошли node --check обоих листов, разрешение относительных импортов и сверка ru/en: добавлен один общий ключ, все прежние значения сохранены.

Справочник: check --freshness — valid/current; адресный тест .025 контекста добычи — OK. Проверки ссылок, git diff --check и сохранения метаданных доступа прошли.

Артефакты: `/var/lib/foundryvtt/Data/work-cat/witcher-loot-fixes-20260917` — `loot.test.mjs`, `offline-tests.tap`, `static-checks.json`. Игровые проверки не запускались; служба, права и базы компедиумов этим исправлением не менялись. Соседние 00219–00225 остаются за пределами блока.

Связанные исправления: [00206](issue-00206.md#implementation), [00208](issue-00208.md#implementation), [00218](issue-00218.md#implementation). [Общая очередь игровой приёмки](issue-00206.md#acceptance-plan).

| Поле | Значение |
| --- | --- |
| ID | issue-00206 |
| Дата регистрации / проверки | 2026-09-11 |
| Ветка и коммит | `rusbar-main`, `8b938d44a042749df027d8b58e28bb1d79638091` |
| Версия / окружение | Foundry 14.367.0, Node 24.16.0; изолированный запуск без мира и БД |
| Статус | open — исправление согласовано пользователем 2026-09-17 |
| Связанная задача | [TASK-0003.032](../../tasks/task-0003.032.md) — анализ, задача исправления не создана |

## Общее описание проблемы

getOrCreateFolder считает первым элементом CONST.FOLDER_DOCUMENT_TYPES тип Actor. В установленной Foundry 14.367.0 первым стал ActiveEffect, поэтому обработчик ищет или создаёт папку эффектов и передаёт её ID в Actor.create.

## На что влияет в системе

Выбор папки при экспорте монстра в добычу; соответствие типа папки создаваемому документу.

## Почему это проблема

Комментарий //actor и создаваемый type='loot' относятся к Actor, но фактический type папки — ActiveEffect. Существующая одноимённая Actor-папка пропускается.

## Где конкретно локализована в коде

| Файл | Локализация |
| --- | --- |
| [module/actor/sheets/WitcherMonsterSheet.js](../../../module/actor/sheets/WitcherMonsterSheet.js) | getOrCreateFolder: 153–166; #exportLoot: 181–187 |

## Предложение по изменению

После согласования указывать тип папки Actor явно и проверить поиск существующей папки. Исправление не выполнено.

## Воспроизведение и доказательства

Группа 15 с настоящей константой Foundry и методом getOrCreateFolder: при наличии Actor-папки нужного имени запрошено создание type='ActiveEffect'; при наличии такой папки выбран её ID. Группа 16 подтвердила передачу ID в payload Actor.create. Ядро: /opt/foundryvtt/common/constants.mjs: 542. Folder.create/Actor.create подменены, фактическая серверная валидация и размещение в каталоге не проверялись.

[Сценарии и ограничения проверки](../../analytics/code-audit/review-log.md#task-0003032).

## Подтверждение и закрытие

Регистрация разрешена [пунктом 9 TASK-0003](../../tasks/task-0003-remaining-files.md#общие-требования-к-каждой-подзадаче) и поручением продолжить. Подтверждение проблемы, согласование исправления и закрытия не получены. Воспроизведение агентом не заменяет решения пользователя.

## Дополнительная сверка TASK-0003.035

2026-09-11, `rusbar-main`, `1d29f681ffed1c46b9c05b0eff09935300c3bf7d`; исходники не менялись.

Новый consumer экспортированного Actor — полностью описанный WitcherLootSheet. Наличие его карточки/рендера не доказывает прохождения Actor.create с Folder ActiveEffect; ошибка выбора типа папки и границы .032 сохраняются. Core/сеть/БД экспорта заново не запускались.

[Проверки и ограничения](../../analytics/code-audit/review-log.md#task-0003035). Статус остаётся potential; подтверждение, исправление и закрытие не выполнялись.

<a id="acceptance-plan"></a>
## Игровая приёмка после перезапуска — ожидает команды пользователя

1. В QA-мире проверить создание Actor-папки добычи, повторный экспорт в неё и наличие одноимённой папки другого типа. Убедиться, что новый Actor типа loot получает правильный folder.
2. Проверить множители 1/2 и отказ на пустом, 0, отрицательном и дробном вводе; отказ не должен оставлять папку или Actor. Сопоставить обычные количества и формулы, сохранение после reload. Повторить сообщения ru/en.
3. Экспортировать монстра с мутагеном: Item присутствует у нового Actor, виден в листе и сохраняется после reload. Продажу, drag/drop и смену изображения этот шаг не исправляет и не принимает.
4. Завершить полный маршрут 00332 М05: 00207, 00039 и 00040 — последовательный пересчёт, несколько результатов таблицы, повторное попадание в стопку, частичный отказ. Временный обход getOrCreateFolder больше не использовать.
5. Оставшаяся приёмка [установленной criticalWounds .00028](issue-00332.md#installation): три Torn Stomach из настоящего pack используют commonsp (−2/−2/−1); Heart Damage (Treated) даёт +2 bleed с priority60. Проверить применение/сохранение на QA Actor и ход с кровотечением. Это поставка предыдущей итерации, новая сборка не требуется.
6. Удалить созданные QA-документы, восстановить исходные настройки и зафиксировать фактические результаты до предложения закрыть issues.

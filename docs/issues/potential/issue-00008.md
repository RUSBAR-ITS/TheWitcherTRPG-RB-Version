# issue-00008 — Ответ true от query не подтверждает выполнение операции

| Поле | Значение |
| --- | --- |
| ID | `issue-00008` |
| Дата регистрации / проверки | 2026-09-10 |
| Ветка и коммит | `rusbar-main`, `3252300787c348e11f95098c345a6af7704b690c` |
| Версия Foundry | `14.367.0` по `/opt/foundryvtt/package.json` |
| Статус | `potential` — подтверждение пользователя не получено |
| Связанная задача | [TASK-0002](../../tasks/task-0002-system-initialization.md) — обнаружение; задача исправления не создана |

## Общее описание проблемы

Оба обработчика запросов возвращают true после вызова целевой функции, не ожидая её Promise. В ветке методов query вернёт true даже при отсутствии метода на entity и entity.system.

## На что влияет в системе

Подтверждение удалённых операций добавления предмета, улучшения и применения эффектов; передача ошибок вызывающему клиенту.

## Почему это проблема

Успешный ответ может прийти до окончания диалога или записи документа. Ошибка асинхронной операции не связывается с Promise обработчика; отсутствие метода также не отличается от выполненной операции.

## Где конкретно локализована в коде

- [module/setup/queries.js](../../../module/setup/queries.js) — applyTemporaryItemImprovementsToActor, строки 9–12; query, строки 34–43.
- [module/actor/mixins/temporaryEffectMixin.js](../../../module/actor/mixins/temporaryEffectMixin.js) — applyTemporaryItemImprovements ожидает DialogV2.prompt и шаблон сообщения.
- [module/actor/witcherActor.js](../../../module/actor/witcherActor.js) — addItem, строка 259: ожидает update/createEmbeddedDocuments.

## Предложение по изменению

Согласовать контракт ответа (принятие запроса или завершение операции), проверять существование единственной целевой функции и связать её результат/ошибку с ответом согласно выбранному контракту. Для таблицы разрешённых функций учитывать только собственные ключи. Конкретное исправление не согласовано.

## Воспроизведение и доказательства

В исходный query из Node vm передан addItem с управляемым незавершённым Promise: await query вернул true при complete=false; после ручного разрешения операции complete=true. Такой же результат у специального запроса улучшений. Объект без методов на двух проверяемых уровнях также дал true. Дополнительно function='constructor' принят через оператор in у обычного объекта; неизвестное имя вернуло false.

Пределы проверки: Целевые операции в опыте заменены управляемыми заглушками, реальные документы не создавались. В Foundry User.query возвращает ответ обработчика (локальный /opt/foundryvtt/client/documents/user.mjs:283–321); межклиентская доставка не воспроизводилась. Перехват позднего rejection не запускался, его потеря следует из отсутствия await/return.

## Связанные материалы оригинального репозитория

Дата проверки источников: 2026-09-10. Ссылки приведены для справки; они не подтверждают дубликат или исправление этой проблемы.

- [Issue #359 — The "Gift" function is not working properly](https://github.com/witchertrpg-foundryvtt/TheWitcherTRPG/issues/359), закрыта. Сообщается об исчезновении предмета при передаче между игроками, если у получателя ещё нет такого предмета; для GM сценарий работал.

Это справочный пример неуспешной передачи предмета между клиентами. Преждевременный ответ `true`, отсутствие ожидания Promise и связь с текущим обработчиком `query` в этом issue не установлены. Прямой аналог нашей проблемы не найден.

## Подтверждение и закрытие

Регистрация разрешена пользователем в рамках пофайлового исследования. Подтверждение проблемы, разрешение на исправление и подтверждение закрытия не получены. Изменения системы не выполнялись; основание закрытия не определено.

## Уточнение TASK-0003.007

2026-09-10, `b8b89a7e3392235f993c21f3c6d277a4a2e7a55f`. Полностью разобран addItem: он ожидает foundItem.update либо createEmbeddedDocuments и возвращает Promise<undefined>. Изолированный контролируемый update подтвердил, что addItem остаётся pending до его разрешения. Таким образом, преждевременный ответ query не следует из синхронности addItem. Другие Actor-цели query сопоставлены с temporaryEffectMixin и adrenalineMixin; сеть повторно не запускалась.

[Сверка TASK-0003.007](../../analytics/code-audit/review-log.md#task-0003007). Статус potential сохранён; исправления не выполнялись.

## Дополнение TASK-0003.008

2026-09-10, `c5edcbadd05ff4038a174bd2e2a49785e40ea878`. Сверен документ [WitcherItem](../../analytics/code-audit/files/module/item/witcherItem.js.md): restoreReliability существует на его прототипе благодаря repairMixin и делегирует RepairSystem. Вызов Queries не получает ожидание вложенной операции автоматически. Методы realCraft и checkIfItemHasRollTable этим маршрутизатором не разрешены; их собственные проблемы описаны отдельно.

[Сверка](../../analytics/code-audit/review-log.md#task-0003008). Полный сетевой/браузерный сценарий не запускался. Статус potential и отсутствие согласованного исправления сохранены.

## Уточнение TASK-0003.009

2026-09-10, `a33bf33add228ae93f96a52046c8feb4ee992921`; исходники прежнего среза не изменены.

Прослежены целиком applyActiveEffectToTargets, applyActiveEffectToActorViaId, applyActiveEffectToActor и actor.applyTemporaryItemImprovements. Прямой owned applyActiveEffectToActor ждёт обычный createEmbeddedDocuments, но его обёртки ToTargets/ViaId не ждут вызов; отдельная ветвь улучшений также не ожидается. В самой примеси запись эффектов оружия и ChatMessage.create запускаются без await. Контролируемый Promise записи ещё оставался pending после возврата из обёртки или примеси. Общий и отдельный принимающие query по-прежнему возвращают true без ожидания вызванных методов.

Remote-маршрут обычных эффектов посылает также отдельный запрос улучшений со всем списком; обычный query получает UUID и отфильтрованный список. Ошибка передачи длительности выделена в issue-00044, повторной пересылки отсутствующего Item — в issue-00045. Реальная доставка сообщений между клиентами и обработка сетевых ошибок не запускались.

[Сверка TASK-0003.009](../../analytics/code-audit/review-log.md#task-0003009). Статус potential сохранён; подтверждение и исправление не согласованы.

## Уточнение TASK-0003.015

2026-09-10, `7edb814aa870da75c7ad7633536e899a8d07e205`; исходники неизменны. [Сверка](../../analytics/code-audit/review-log.md#task-0003015).

Проверен новый вход через [consumeMixin](../../../module/item/mixins/consumeMixin.js) → applyActiveEffectToActorViaId. Сам consume не ожидает helper, helper не ожидает query. При удалённом в UUID-фасаде источнике перехвачен один GM query; удалённый обработчик не исполнялся. Ранний ответ query из исходного наблюдения здесь не воспроизводился повторно и не считается завершением переноса эффектов.

## Уточнение TASK-0003.017

2026-09-10, `c7cd9d71dcb1714cdccb175aee351a3f1df95c5b`; исходники неизменны. [Сверка](../../analytics/code-audit/review-log.md#task-0003017).

Полностью разобрана цепочка разрешённого restoreReliability: [repairMixin](../../../module/item/mixins/repairMixin.js) → [RepairSystem](../../../module/item/systems/repair.js) → WeaponData/ArmorData.repair. Promise update теряется и на нижних уровнях (issue-00081). Сам штатный _doRepair отправляет emitForGM по системному сокету, не User.query. Его проверка с захваченным сообщением не считается новым воспроизведением ответа true от query. Старое наблюдение и статус potential сохранены.

## Дополнительная сверка TASK-0003.022

2026-09-11, `ef8117ba6e5a184989e65761d47a068381056e4a`; исходники не менялись.

Ответ query снова проверен с настоящей моделью Item/RegionProperties: true не означает вызова нужного метода. Собственные adapters региона тоже завершаются раньше update, а countdown — раньше setFlag/delete. Это не исправление маршрутизации: локальный контракт вынесен в issue142.

[Основной источник](../../../module/setup/queries.js); [методика и результаты](../../analytics/code-audit/review-log.md#task-0003022). Статус остаётся potential, решения об исправлении нет.

## Дополнительная сверка TASK-0003.028

2026-09-11, `9f16a7ae4bc942df85a105fd37d9a3cb3ef99f4b`. Полностью разобран helper.getActorOwner: он выбирает пользователя, но не выполняет и не подтверждает query. При отсутствии активного OWNER/GM ошибка может возникнуть ещё до вызова маршрутизатора — это отдельная [issue-00185](issue-00185.md). Проверены определения getActorOwner, applyStatusEffectToActor и регистратор query; исходный query повторно не исполнялся. Новое наблюдение не устраняет отсутствие await у callableFunctions/методов entity.

Результаты и ограничения — [сверка TASK-0003.028](../../analytics/code-audit/review-log.md#task-0003028). Статус potential сохранён; исправления не выполнялись.

## Дополнительная сверка TASK-0003.030

2026-09-11, `aa6af106e86a9c75fe050d599f961c8fadb74f1b`. Дополнительный полностью разобранный callableEntityFunctions consumer — adrenalineMixin.addAdrenaline. При optional=true он сам не ожидает Actor.update; query не ожидает его Promise. Проверен изолированный pending update, сетевой путь defenseMixin→getActorOwner→query не запускался.

[Результаты и пределы проверки](../../analytics/code-audit/review-log.md#task-0003030). Статус potential сохранён; подтверждение пользователя и согласование исправления/закрытия не получены.

## Дополнительная сверка TASK-0003.038

2026-09-11, `rusbar-main`, `b47ba02cdaebc6a66ad14a5638213b6eb24460b4`; исходники не менялись.

Способность HP отправляет query без await; registry вызывает applyActiveEffectToActor также без ожидания. Группа 17 удерживала Promise query, пока doProfessionSkillUsage уже завершился. Это конкретный producer к прежнему наблюдению, не доказательство серверной ошибки. Отдельные потери Promise в dispatcher/weapon/threshold описаны в 241.

[module/actor/mixins/professionMixin.js](../../analytics/code-audit/files/module/actor/mixins/professionMixin.js.md), [templates/partials/character/tab-profession.hbs](../../analytics/code-audit/files/templates/partials/character/tab-profession.hbs.md), [templates/sheets/actor/partials/monster/tabs/tab-profession.hbs](../../analytics/code-audit/files/templates/sheets/actor/partials/monster/tabs/tab-profession.hbs.md), [templates/dialog/combat/profession-attack.hbs](../../analytics/code-audit/files/templates/dialog/combat/profession-attack.hbs.md).

[Сверка и ограничения](../../analytics/code-audit/review-log.md#task-0003038). Связанные файлы повторно в покрытии не учитывались; код и статусы issues не изменены.

Статус potential; подтверждение пользователем, согласование исправления и закрытия не получены.

## Дополнительная сверка TASK-0003.039

2026-09-11, `c598d74e34f4be51535de78b38f0601c286c5407`; исходники не менялись.

Группа 33 исполнила полный castSpell с actor.isOwner=false и реальными effect/status helpers, захватив запросы applyStatusEffectToActor/applyActiveEffectToActor. Query заменён; успешное завершение castSpell и наличие отправленного payload не доказывают выполнение у владельца. Собственная граница ожидания castSpell описана в issue-00251.

[Результаты и пределы проверки](../../analytics/code-audit/review-log.md#task-0003039). Статус potential сохранён; подтверждение пользователем, согласование исправления и закрытия не получены.

## Дополнительная сверка TASK-0003.040

2026-09-11, `rusbar-main`, `74322e91edac106c82668f4a47eef53ce1889dc1`. Схемы сообщений не меняют контракт query. В этой порции сравнивались подготовленные данные и локальные callbacks; обработчики двух клиентов не запускались. Предыдущие наблюдения об отсутствии общего ожидания query остаются применимыми, новых выводов о доставке нет.

[Методика, результаты и ограничения](../../analytics/code-audit/review-log.md#task-0003040). Исходники не менялись. Статус остаётся potential; подтверждения пользователем, исправления и закрытия нет.

# issue-00001 — Возможное несоответствие регистрации системы в Foundry

| Поле | Значение |
| --- | --- |
| ID | `issue-00001` |
| Дата регистрации | 2026-09-10 |
| Дата последней проверки | 2026-09-10 |
| Ветка | `rusbar-main` |
| Коммит | `059f0c775dc47914dcad4b50167c934ad2f44929` |
| Версия Foundry VTT | `14.367.0` по `/opt/foundryvtt/package.json`; запущенный экземпляр в этой проверке не проверялся |
| Окружение | Каталог системы `/var/lib/foundryvtt/Data/systems/TheWitcherTRPG-RB-Version`; исследованы локальные файлы |
| Связанная задача | Не создана |

Статус — `potential`, как указано в [реестре](../README.md). В рабочем дереве есть незакоммиченные изменения документации; `system.json` относительно указанного коммита не изменён.

## Общее описание проблемы

Имя каталога установленного checkout — `TheWitcherTRPG-RB-Version`, а поле `id` в [system.json](../../../system.json) содержит `TheWitcherTRPG`. Это различие проверено по локальному пути и манифесту.

Наличие ошибки обнаружения, регистрации или загрузки системы в работающем Foundry не установлено. Карточка фиксирует возможную проблему, а не подтверждённый сбой.

## На что влияет в системе

Подтверждённое влияние на игровые сценарии пока не установлено.

Предположительно различие может затрагивать обнаружение системы и сопоставление её идентификатора с каталогом ресурсов. Возможный конфликт с другой установленной системой с тем же ID также требует отдельной проверки; наличие такого конфликта не подтверждено.

## Почему это проблема

Ожидается, что Foundry однозначно обнаруживает установленную систему, связывает её с нужным каталогом и загружает её ресурсы. Пока не проверено, мешает ли этому наблюдаемое различие в текущем окружении.

Чтобы подтвердить проблему, нужно установить фактическое поведение регистрации в используемой версии Foundry и показать конкретный сбой либо конфликт. Одного различия строк недостаточно для вывода о неисправности.

## Где конкретно локализована в коде

- [system.json](../../../system.json), строка 2: поле `id` со значением `TheWitcherTRPG`.
- Каталог checkout в файловой системе: `/var/lib/foundryvtt/Data/systems/TheWitcherTRPG-RB-Version`. Его имя не является отдельным полем исходного кода системы.

Это места, где зафиксировано различие. Причина возможной ошибки и конкретный обработчик регистрации Foundry пока не локализованы. Полный перечень зависимых путей и идентификаторов в коде системы в рамках регистрации этой карточки не исследован.

## Предложение по изменению

Техническое решение пока не выбрано. Предлагается сначала согласовать проверку регистрации системы и загрузки ресурсов на установленной версии Foundry.

Если влияние подтвердится, определить согласованные ID системы и каталог установки, проверить зависимые пути, ссылки на документы и настройки выпуска, затем предложить конкретный перечень изменений. Переименование каталога или массовая замена ID сейчас не согласованы. Если различие не вызывает проблемы, представить результаты пользователю для решения о закрытии карточки.

Приоритет не назначен; задача на исправление не создана.

## Воспроизведение и доказательства

Выполнена проверка файлов, без запуска игрового сценария:

1. Установлен абсолютный путь текущего checkout; имя каталога — `TheWitcherTRPG-RB-Version`.
2. Прочитан `system.json`; значение поля `id` — `TheWitcherTRPG`.
3. Проверены рабочая ветка и коммит командами Git; манифест не имеет незакоммиченных изменений.
4. Прочитан `/opt/foundryvtt/package.json`; поле `version` — `14.367.0`.

**Ожидаемый результат работы системы:** однозначное обнаружение и корректная загрузка ресурсов установленной системы. Выполнение этого ожидания в работающем Foundry здесь не проверялось.

**Фактический результат проверки:** подтверждено различие имени каталога и ID манифеста. Ошибки Foundry, журналы службы и браузерные запросы в рамках этой проверки не исследованы; воспроизводимый сбой не получен.

**Предлагаемая отдельная проверка:** выяснить, как Foundry регистрирует этот каталог, какой ID отображается в списке систем и по каким адресам загружаются его ресурсы; при наличии ошибки установить связь с наблюдаемым различием. Это план проверки, а не выполненные действия.

## Подтверждение и закрытие

| Решение | Дата и результат |
| --- | --- |
| Согласование регистрации пользователем | 2026-09-10: согласовано оформление наблюдения как `issue-00001` в `potential` |
| Подтверждение проблемы пользователем | Не получено |
| Согласование исправления | Не получено; техническое решение не выбрано |
| Подтверждение закрытия пользователем | Не получено |

- **Основание закрытия:** не определено.
- **Выполненные изменения:** исправления системы не выполнялись; создана карточка наблюдения.
- **Проверки результата исправления:** не выполнялись, исправления нет. Выполненные проверки исходного наблюдения описаны выше.

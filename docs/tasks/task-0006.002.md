# TASK-0006.002 — Локальный поиск и проверка записей

| Поле | Значение |
| --- | --- |
| Статус | `done` |
| Создана / обновлена | 2026-09-14 |
| Основание | Пользователь поручил составить задачу и первые подзадачи создания справочника-графа текущей системы |
| Срез планирования | rusbar-main, cdebc3f69c642180f36979afd451e19fdc8339d6; при исполнении проверить актуальность |
| Родительская задача | [TASK-0006](task-0006-system-index.md) |
| Зависимости | [TASK-0006.001](task-0006.001.md) и согласованный результат выбора формата |

## Проблема и результат

Согласованное представление требуется превратить в работающий инструмент: читать записи, находить сущность, раскрывать её связи и сообщать о неполноте данных. Создать минимальный локальный поиск и валидатор с небольшим проверенным набором из трёх файлов .001.

Результат — воспроизводимый запуск из репозитория по документированной команде. Формат и размещение .001 согласованы пользователем; используется Python 3 со стандартной библиотекой. Автоматический семантический разбор всего JavaScript не является обязательной частью первого инструмента.

## Требования и порядок работы

1. Проверить согласованный контракт .001 и зафиксировать перечень создаваемых файлов. Подготовить загрузку, проверку схемы и ссылок без импорта или исполнения игрового кода.
2. Использовать начальные записи из примеров .001; подтвердить их адреса и версии. Проверить различение value/label у stat() и Skill. activeEffectModifiers в этой выборке определено только у Skill; вложения модели не создавать как новые определения этого поля.
3. Реализовать поиск по ID, точному символу/пути и терминам; неоднозначный запрос возвращает различимые варианты. Чувствительность к регистру и работа с русским текстом документируются.
4. Реализовать входящий/исходящий обход, фильтр типа связи, чтение/запись поля, обращение к процессу/подробностям. Интерфейс IQ-07 может возвращать «процесс ещё не индексирован», пока записи процессов не заполнены; это не считается содержательным покрытием запроса.
5. Ограничивать глубину/объём вывода, отмечать усечение и давать способ продолжить. Цикл не вызывает бесконечный обход; запрос не меняет индекс.
6. Валидировать ID, типы, концы связей, владельцев, шаги/переходы, адреса и сведения о версии. Разрешённая внешняя граница отличима от повреждённой внутренней ссылки.
7. Добавить необходимые проверки инструмента, краткую инструкцию запуска и несколько примеров вывода. Сопоставить ожидаемые реальные записи с исходниками, а не вычислять ожидаемый ответ той же функцией поиска.

## Проверочные случаи

| Случай | Ожидаемое свойство |
| --- | --- |
| Точный ID/символ/путь | Возвращается адресуемая запись с версией и подробностями |
| value / label | Одноимённые поля stat() и Skill различаются по владельцу |
| activeEffectModifiers | В начальных трёх файлах одно определение у Skill; 13 вложений не размножают его |
| Обратный поиск | Использует ту же связь и место обращения, что прямой |
| Пустой результат / неохваченный участок / динамическая цель | Три состояния явно различимы |
| Цикл и предел обхода | Завершение, отметка усечения и возможность раскрытия |
| Дублированный ID, потерянный конец связи, неверный переход | Валидатор сообщает конкретную запись и причину |
| Поиск и проверка | Исходники, индекс, игровые документы и права не меняются |

Ошибочные записи допускаются только как отдельные тестовые входы инструмента, не как сведения о реальной системе.

## Файлы

По [согласованному формату .001](../analytics/system-index/format.md#файлы-реализации-002): docs/analytics/system-index/query.py, tests/test_query.py и tests/fixtures/ относительно той же папки. Используются manifest.json, sources.jsonl, examples/*.jsonl и examples/queries.json; обновляются README.md, query-contract.md и review-log.md. Точный контракт — в [спецификации запросов](../analytics/system-index/query-contract.md). query.py, tests/test_query.py и tests/fixtures/invalid-records.json созданы; существующие JSONL-примеры используются без изменения. Обновляются указатели и статусы задач; package.json, сборка системы, packs и служба Foundry не затрагиваются.

## Критерии приёмки

- [x] Поиск и валидатор запускаются локально по инструкции с согласованным форматом.
- [x] Все восемь видов запросов имеют определённый интерфейс; фактически заполненные и ещё не заполненные ответы различены.
- [x] Начальный набор из трёх файлов проверен по коду, содержит прямые/обратные связи и неоднозначное имя.
- [x] Ошибочные записи выявляются; цикл, ограничение выдачи и пустые результаты обработаны.
- [x] Проверки инструмента пройдены; запросы не исполняют игровой код и не меняют читаемые данные.
- [x] Фактические команды, вывод и границы сохранены; .003 может расширять данные без смены смысла запросов.

## Итог

Выполнена 2026-09-14 на e414bb9fa7321e5a13e7c147b380c061dbbdb318, rusbar-main. Созданы [query.py](../analytics/system-index/query.py), [тесты](../analytics/system-index/tests/test_query.py) и [24 искусственных повреждения входов](../analytics/system-index/tests/fixtures/invalid-records.json). Поиск поддерживает все восемь запросов, входящий/исходящий обход, фильтры, циклы, страницы, процессы, подробности и проверку актуальности. Формат согласован: manifest.approval=approved; решение зафиксировано как DEC-0001.

Прошли 16 тестовых методов, включая все 16 CLI-примеров .001, проверку ошибок, границ и сохранности. check --freshness подтвердил 615 исходников, 618 документов и внешнюю версию. [Протокол исполнения](../analytics/system-index/review-log.md#task-0006002) содержит команды и ограничения, [README](../analytics/system-index/README.md) — запуск.

Начальный набор сохранён: 35 сущностей, 53 связи, два процесса; три источника partial, остальные 612 not_indexed. TASK-0006 остаётся in-progress; следующая .003 planned. Игровой код, существующий аудит, issues и права не изменялись.

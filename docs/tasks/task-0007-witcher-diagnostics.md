# TASK-0007 — Диагностический модуль Witcher: журнал событий и снимок состояния

| Поле | Значение |
| --- | --- |
| Статус | `in-progress`: модуль 0.1.0 создан и проверен; постоянное подключение ожидается |
| Создана / обновлена | 2026-09-16 |
| Основание | Пользователь поручил расписать создание клона и улучшение имеющегося диагностического модуля для Witcher, включая снимок текущего состояния одной командой |
| Исходная версия | `dev`, `af4d6f5fc7b40279f09eb66b0ca58aa2e1469ab4`, система 14.3.1.00016; оформление задачи — .00017 |
| Окружение | Foundry 14.367.0, Node.js 24.16.0; модуль-источник wod-v20-v7-translate 0.1.70 |
| Зависимости | Исходники диагностики WoD; для автоматической браузерной приёмки — TASK-0008. Разработка модуля не требует готового браузерного стенда |
| Текущее поручение | Пользователь разрешил создание модуля; код и проверки выполнены в .00019, постоянная служба/nginx и включение в мире ещё не применены |

## Проблема и результат

Серверные логи Foundry не дают полного контекста действий браузера и подготовленных данных персонажа. Сейчас для Witcher нет подключённого инструмента, позволяющего получить такой контекст на сервере одной командой. Для другой системы уже существует клиентский сбор событий с доставкой в отдельную Node-службу.

Результат: самостоятельный диагностический модуль для `TheWitcherTRPG-RB-Version`. Пользователь выбирает персонажа/токен, выполняет одну команду и получает ID отчёта. Отчёт появляется на диске сервера и доступен для последующего анализа агентом. Снимок работает и при выключенном непрерывном журнале. Он показывает состояние в момент снятия; восстановление прежнего состояния до ошибки без заранее собранной истории не обещается.

Пример: после изменения эффекта персонаж показывает неожиданную скорость. Снимок должен позволить сопоставить исходные поля Actor, подготовленные характеристики, вложенные Items, собственные и предметные эффекты, состояния включения/подавления и настройки расчёта. Сам диагностический модуль не исправляет скорость и не воспроизводит вместо системы её формулы.

## Исследованная основа

Источник расположен в `/var/lib/foundryvtt/Data/modules/wod-v20-v7-translate`; HEAD при оформлении — `7c12ff0f71b64800297c81e2f620c6d7df819a27`. Перед выделением кода зафиксировать фактические хеши используемых файлов и сведения об авторстве.

| Существующий файл источника | Что можно использовать | Что пересмотреть для Witcher |
| --- | --- | --- |
| `scripts/client-capture.js` | Очередь событий, sessionId, console/error/unhandledrejection, транспорт, ограничения сериализации | Namespace, настройки, состав данных, корректное включение/выключение; сетевые обёртки не переносить автоматически целиком |
| `scripts/logger/core.js` | Уровни и доставка структурированных сообщений | Объединить доставку с клиентским сбором, исключить двойную запись одного сообщения |
| `scripts/debug-hooks.js` | Примеры подписки на окна и документы | Убрать метрики/селекторы WoD; использовать штатные события Foundry 14 и отдельный адаптер Witcher |
| `scripts/settings.js` | Переключатели диагностики | Собственные настройки и совместимый с Foundry 14 интерфейс; переводные настройки WoD не клонировать |
| `collector/client-log-collector.mjs` | HTTP-приём и запись JSONL | Отдельные endpoint/каталог, проверка формата и допуска, безопасное построение путей; существующий `/clear` всего журнала не переносить |
| `collector/systemd/…`, `collector/nginx/…`, `collector/README.md` | Пример серверного размещения | Новая служба и согласованная конфигурация, без замены действующего сборщика WoD |

При предыдущей проверке служба `wod-v20-v7-client-log-collector` была active; её конфигурация задаёт `127.0.0.1:30120`, а код пишет в `/var/lib/foundryvtt/Logs/wod-v20-v7-client`. Это свидетельство существующей основы, не подтверждение доставки событий Witcher.

## Требования

### 1. Выделение самостоятельного модуля

- Клонировать диагностическую часть, сохранив сведения о её происхождении. Не переносить переводы, изменения листов, механики или хуки WoD, не относящиеся к диагностике.
- Не подключать исходный модуль WoD к миру Witcher и не изменять его код/службу при реализации новой задачи.
- Поддерживать текущий Foundry 14 и текущий ID форка. Совместимость со старыми системами и миграциями в задачу не входит.
- Предлагаемые имя и место: `witcher-trpg-diagnostics`, каталог `/var/lib/foundryvtt/Data/modules/witcher-trpg-diagnostics`. Это проектное имя до реализации.
- Предлагаемый лог-каталог: `/var/lib/foundryvtt/Logs/witcher-trpg-diagnostics`. Сведения о мире, пользователе и сессии входят в каждую запись; размещение и права определяются при согласовании установки.

### 2. Снимок одной командой

Предлагаемый публичный контракт:

```js
await game.modules.get("witcher-trpg-diagnostics").api.captureSnapshot({
  actorUuid: "Actor.…",
  reason: "Неверная скорость после включения эффекта"
});
```

Предусмотреть вызов без UUID для ровно одного управляемого токена и короткий макрос для панели. При отсутствии/множественном выборе вернуть понятное объяснение, а не угадывать Actor. Явный `tokenUuid` должен выбирать Actor соответствующего токена, включая непривязанного; нельзя молча подменять его исходным Actor из каталога. Окончательные имена API и доступ из заголовка карточки уточняются при реализации; отдельный большой интерфейс не требуется.

Команда возвращает структурированный результат: snapshotId, состояние сбора, состояние доставки/сохранения, сведения о неполных разделах. Успех записи на сервере показывается после подтверждения сборщика. При недоступности сборщика собранный снимок остаётся доступен локально для скачивания; отказ доставки не объявляется успехом серверной записи.

| Раздел снимка | Обязательные данные и различия |
| --- | --- |
| Контекст | schemaVersion, snapshotId, sessionId, время начала/окончания, причина, worldId, userId/роль, язык, Foundry/system/module версии, сцена и tokenUuid при наличии |
| Actor | UUID, тип, исходные сериализованные поля и отдельный раздел текущих подготовленных значений system; характеристики, навыки, производные показатели, ресурсы и боевые поля |
| Токен | UUID/sceneId, actorLink, связь с базовым Actor и фактический Actor токена; отсутствие сцены/выбора отражается явно |
| Items | UUID/ID, тип, источник/родитель, исходные и относящиеся к диагностике подготовленные поля; equipped/isActive и травматические treatment/location/followUp там, где они есть |
| ActiveEffect | Эффекты самого Actor и Items с владельцами, disabled/transfer/statuses, origin, duration и прочитанными состояниями active/suppressed/expired; изменения в фактическом формате Foundry 14, включая `system.changes` и phase, а также специфические поля Witcher |
| Применимость | Отдельно собственные эффекты, эффекты предметов и набор, который текущий Actor считает применимым. Ошибку чтения getter записывать как ошибку раздела, не заменять значением false |
| Настройки | Зарегистрированные настройки Witcher и диагностического модуля; относящиеся к сценарию core/внешние настройки по явному списку. Для каждой указывать namespace/key, scope, доступное эффективное значение или причину недоступности |
| Модули | ID, версия, включённость, сведения о текущем наборе расширений; содержимое иных модулей не выгружается целиком |
| Компедиумы | Объявленные пакеты и реально присутствующие `game.packs`, collection/type/package, доступность/locked, состояние индекса, известные поля/размер индекса и число документов в клиентском кэше — отдельно |
| Полнота | Статус каждого раздела: собран / недоступен / ошибка / не загружен / усечён; причины, лимиты и число пропущенных элементов, если известно |

В базовом снимке не загружать автоматически содержимое всех компедиумов. Пустой кэш документов или не загруженный индекс не означает пустую БД. Отдельный режим явной проверки выбранного пакета может запросить индекс/один документ с фиксацией результата и времени; это чтение, не запись пакета.

Снимок сериализует текущие данные без вызова `update`, `updateSource`, принудительного `prepareData/reset` или повторного применения эффектов. Пока сбор идёт, мир может меняться: отмечать границы времени и наблюдённые обновления, не обещать атомарный снимок серверной БД. Он отражает доступное конкретному клиенту состояние, а не данные всех игроков.

### 3. Базовый журнал событий

- Переключатель сбора; снимок по запросу работает независимо от него.
- Ошибки браузера/Promise и сообщения console с сохранением исходного поведения console; события открытия/закрытия окон, создания/обновления/удаления документов и выбранные действия интерфейса.
- Сохранять UUID документа, идентификатор окна/действия, изменения полей, клиентский порядковый номер, sessionId, клиентское и серверное время. Инициатора обновления и клиента-наблюдателя различать, чтобы одинаковые сетевые события не считать несколькими действиями пользователя.
- Предварительный запрос изменения и подтверждённое обновление отмечать разными типами событий. Временная близость клика и обновления не доказывает причинную связь; надёжную связь указывать только при наличии соответствующего идентификатора/контекста.
- Очередь ограничена; доставка пакетная и не задерживает игровые действия. Счётчики пропуска, усечения и ошибок доставки доступны в диагностике. Не маскировать потерю событий.
- Подписки не дублируются после повторной инициализации. При выключении прекращается запись и снимаются собственные обработчики/обёртки, где это предусмотрено; чужие обработчики сохраняются.
- Полный трассировщик всех функций, перехват всех сетевых payload и постоянная запись полного Actor на каждый клик не входят в первую версию.

### 4. Запись и эксплуатация

- JSON для целого снимка, JSONL для событий; форматы имеют версию. Сборщик подтверждает ID принятого отчёта; агент должен иметь возможность прочитать именно этот отчёт после подтверждения.
- Отдельная Node-служба за существующим HTTPS reverse proxy. Конкретные endpoint/порт/проверку допуска согласовать при реализации. Порт 30120 уже занят службой WoD; не подменять её.
- Ограничить размер запроса/снимка, частоту и объём хранения. Усечение диагностических данных всегда обозначать. Предусмотреть понятную ротацию/срок хранения без очистки чужих журналов.
- Не выгружать пароли, cookies, токены и произвольное содержимое всех настроек. Путь файла строит сервер; сырые значения метаданных не могут стать произвольным путём, включая `.`/`..`.
- Не копировать бесконтрольный endpoint очистки и wildcard CORS исходного сборщика. Проверка server-side допуска к отправке не заменяется присланным клиентом `isGM` или одним Origin. Это адресные требования к улучшению имеющейся реализации; точный способ будет зафиксирован до развёртывания.
- Модуль читает только доступные текущему пользователю документы. Снимок чужого Actor без разрешения не должен становиться способом обхода Foundry ownership.
- Развёртывание описать воспроизводимо с учётом существующего Ansible. Пользователь управляет правами/владельцами; агент не меняет их и не расширяет доступ автоматически.

## Границы и зависимости

Требуется диагностический инструмент, а не рефакторинг игровой системы. Ошибки, наблюдаемые инструментом, не исправляются скрыто в его коде. Существующие [issue-00320](../issues/potential/issue-00320.md), [issue-00328](../issues/potential/issue-00328.md), [issue-00036](../issues/potential/issue-00036.md) остаются отдельными механиками.

До начала кода определить размещение исходников/порядок версионирования отдельного модуля. Создание удалённого репозитория и публикация автоматически не входят в задачу. Исходный WoD-модуль остаётся контрольной основой, а не зависимостью времени исполнения.

[Браузерный стенд TASK-0008](task-0008-foundry-browser-tests.md) позднее проверит команду снимка и сохранит snapshotId вместе с тестовым отчётом. Нужны две независимые приёмки: воспроизведение в браузере пользователя → запись сборщиком и автоматический сценарий в браузере агента → запись сборщиком. Работа одного клиента не доказывает подключение другого.

## Этапы реализации

1. Зафиксировать используемые исходники WoD, выделить диагностические компоненты, определить собственный namespace и формат отчёта. Согласовать конкретную установку службы/доступ, не запускать старый установщик целиком.
2. Сделать сбор снимка в памяти, явный выбор Actor/Token и сериализацию с отчётом о полноте; добавить адаптер текущих моделей Witcher.
3. Подключить единый транспорт снимков/событий и отдельный сборщик; проверить подтверждение записи, отказ и локальное сохранение снимка.
4. Добавить базовые наблюдатели событий и настройки en/ru; проверить выключение, повторное подключение и отсутствие изменений игровых документов.
5. Подготовить инструкции и конфигурации для развёртывания, выполнить согласованную установку. Проверить реальный запрос из браузера через HTTPS и доступ агента к полученному файлу.
6. Пройти контрольные сценарии в тестовом мире, обновить документы по фактической реализации. Сведения о выполнении записать здесь; наблюдения не превращать в новые задачи без согласования.

## Файлы

Основные компоненты ниже созданы в модуле 0.1.0; серверные конфигурации подготовлены, но не установлены. Фактический состав и результаты — в разделе реализации ниже.

| Файл или каталог | Изменение | Причина |
| --- | --- | --- |
| `Data/modules/witcher-trpg-diagnostics/module.json`, `README.md` | Новый манифест, границы совместимости и инструкция | Отдельный устанавливаемый модуль |
| `scripts/main.js`, `scripts/settings.js` | Инициализация, переключатели и API | Управление жизненным циклом диагностики |
| `scripts/snapshot.js`, `scripts/witcher-adapter.js` | Сбор структуры и актуальных полей Witcher | Снимок без изменения Actor |
| `scripts/events.js`, `scripts/transport.js`, `scripts/serialize.js` | Наблюдатели, доставка, ограничения/полнота | Один транспорт и воспроизводимый формат |
| `lang/en.json`, `lang/ru.json` нового модуля | Подписи команды, состояний и ошибок | Согласованные две локализации |
| `collector/client-log-collector.mjs`, `collector/README.md` нового модуля | Приём, запись, подтверждение, эксплуатация | Доступный на сервере результат |
| Конфигурация systemd/nginx или согласованная роль Ansible | Подготовить отдельно от действующего WoD-сборщика | Развёртывание воспроизводится без изменения Foundry/прав по умолчанию |
| `tests/` нового модуля | Адресные проверки сериализации, снимка и транспорта | Существенные крайние случаи и защита от изменения документов |
| `docs/documentation/witcher-diagnostics.md` системы | Создать по фактическому API/формату и командам | Краткое руководство, чтение и ограничения отчёта |
| Эта задача, `docs/tasks/README.md`, `docs/README.md`, `docs/CHANGELOG.md` | Обновить фактический результат | Навигация и состояние работы |

Модели/потребители для чтения и сверки, **без запланированной правки**: [witcherActor.js](../../module/actor/witcherActor.js), [witcherActiveEffect.js](../../module/activeEffect/witcherActiveEffect.js), [witcherActiveEffectData.js](../../module/data/activeEffects/witcherActiveEffectData.js), [criticalWoundData.js](../../module/data/item/criticalWoundData.js), [settings.js](../../module/setup/settings.js), [system.json](../../system.json). Имеющиеся [карточки файлов](../analytics/code-audit/files/README.md) и [справочник](../analytics/system-index/README.md) — навигация к актуальному коду, не замена проверки runtime.

При изменении документации системы повысить её счётчик по [правилу версии](../rules/repository.md#версия-системы) и обновить зависимые хеши. Не добавлять модуль автоматически в прежний реестр 615 файлов системы.

## Критерии приёмки и проверки

| Сценарий | Наблюдаемый результат |
| --- | --- |
| Снимок при выключенном журнале | Одна команда → отчёт на диске, читаемый агентом, и подтверждённый snapshotId |
| Actor без Items/эффектов | Валидные пустые разделы, не ошибка сбора |
| World Actor, привязанный и непривязанный токен | Выбран правильный объект; UUID/родитель/подготовленные данные не смешаны |
| Два токена выбраны / ни одного | Понятный ответ; произвольный персонаж не выбран |
| Эффект выключен/подавлен/истёк, Item не экипирован | Отдельно записаны фактические исходные и вычисленные состояния; ошибка getter заметна |
| Новый системный changes/phase | Реальный формат Foundry 14 отражён в снимке; нет предположения, что старый JSON компедиума равен текущей модели |
| Пустой / не загруженный / недоступный пакет | Состояния различаются; нет автоматического вывода «контент потерян» |
| Сборщик недоступен / запрос слишком велик | Игровое действие продолжает работать; ошибка доставки/полноты видна, снимок можно сохранить локально |
| Повторный запуск/выключение журнала | Один обработчик на событие; отсутствует двойная запись через console и транспорт |
| Изолированный вызов снимка | Нет вызовов записи Actor/Item/ActiveEffect и нет изменений сравниваемых исходных данных |
| Обычный игрок | Нет обхода ownership; недоступность разделов отражена явно |
| Браузер пользователя и браузер стенда | Две различимые сессии/отчёта действительно дошли через используемый маршрут HTTPS |

- [x] Зафиксирована основа клонирования; отсутствуют исполняемые изменения WoD.
- [x] Все обязательные разделы снимка и различия состояний реализованы.
- [x] Подтверждены запись настоящим временным сборщиком и независимое чтение файлов агентом.
- [ ] Подтверждены постоянная служба, штатная доставка через HTTPS и снимок из браузера пользователя.
- [x] Базовое логирование выключается, ограничено по объёму и не изменяет игровые действия.
- [x] Приёмка разделяет локальные тесты, временный HTTP-сборщик и работу в реальном мире; штатное развёртывание остаётся непроверенным.
- [x] Обновлены инструкции, реестры и запись об итогах создания модуля.

Полный прогон тестов справочника системы не требуется. Адресные проверки описания/хешей — по [правилу тестирования](../rules/testing.md#объём-проверок-справочника). Реальную разработку диагностического модуля сопровождают его собственные тесты и браузерные сценарии.

## Реализация 0.1.0 — 2026-09-16 / система 14.3.1.00019

Модуль создан в `/var/lib/foundryvtt/Data/modules/witcher-trpg-diagnostics`. Начальная версия модуля — 0.1.0; версия системы .00019 отражает документацию этой работы, игровой движок не менялся. Отдельный Git-репозиторий/публикация не создавались. Исходный модуль WoD, его конфигурации и служба сохранены; восемь хешей исходных файлов сверены с `provenance.json`.

### Фактический состав

- `module.json`, `package.json`, `.gitignore`, `README.md`, `provenance.json`: манифест Foundry 14, две локализации, Node 24, происхождение и эксплуатация.
- `scripts/constants.js`, `serialize.js`, `witcher-adapter.js`, `snapshot.js`: ограничения/редактирование секретных полей, исходные и подготовленные данные, фактические getter навыков/эффектов, настройки, компедиумы, Actor/Token/ownership, полнота.
- `scripts/transport.js`, `events.js`, `api.js`: ключ в памяти вкладки, подтверждённая доставка, локальное сохранение трёх снимков, ограниченная очередь, повтор без дубликатов, отдельные инициатор/наблюдатель.
- `scripts/main.js`, `settings.js`, `lang/en.json`, `lang/ru.json`, `macros/snapshot.js`: жизненный цикл, настройки, меню, команды заголовка V1/V2 и макрос.
- `collector/client-log-collector.mjs`: Node HTTP, отдельный ключ приёма, allowlist мира/системы, лимиты, JSON/JSONL, подтверждение после записи/sync, ротация собственного формата; без endpoint чтения или очистки.
- `collector/systemd/witcher-trpg-diagnostics.service`, `nginx/witcher-trpg-diagnostics.conf`, `config.example.env`, `ansible/service.yml`, `ansible/nginx-extra.example.yml`, `collector/README.md`: подготовленное развёртывание; ничего не применялось к `/etc` и Ansible-репозиторию.
- `tests/fixtures.mjs`, `snapshot.test.mjs`, `events.test.mjs`, `collector.test.mjs`: 14 адресных Node-тестов; отдельный браузерный сценарий находится среди артефактов приёмки.

[Краткая инструкция в системе](../documentation/witcher-diagnostics.md); подробный API/установка — в README самого модуля и collector/README.md.

### Проверки

| Проверка | Результат и граница |
| --- | --- |
| `npm test` в модуле | 14/14: отсутствие записи при сборе, исходные/подготовленные значения, getter ошибки, выбор токена, права, пределы/секреты, очередь/отключение, настоящий HTTP/диск, отказ, повтор/смена даты и ротация. Foundry-модели в Node — фасады |
| Chromium 153.0.8010.12, мир witcher-rb-test | Вход QA-GM/QA-Player; текущая система .00018, модуль 0.1.0. Настоящие Actor/Item/ActiveEffect/Scene/Token и API ядра; файлы модуля и маршрут HTTP временно поданы через Playwright |
| Снимки GM | Чистый персонаж, персонаж с Item и предметным/собственным эффектами, монстр, привязанный и непривязанный токены; исходники Actor до/после одинаковы; все разделы collected без ошибок/усечений |
| Компедиум | Явный probe criticalWounds получил 94 записи; базовый снимок не запрашивает загрузку всех библиотек |
| Обычный игрок | Снимок своего Actor записан, чужого Actor — отказ до сбора/доставки |
| Диск/локальный резерв | Семь JSON-снимков и пакет JSONL прочитаны агентом; без ключа отчёт сохранился локально и скачался через настоящий браузерный download |
| Журнал/меню | Событие console доставлено, запись выключена; меню ru отрисовано, screenshot сохранён; pageerror нет |
| Cleanup | Три временных Actor и сцена с двумя токенами удалены только по собственным ID/runId; оставшихся тестовых документов нет |
| Конфигурации | Ansible syntax-check, systemd-analyze verify, JavaScript/JSON и одинаковый набор en/ru ключей прошли. Nginx-фрагмент проверен в изолированном конфиге с отключённым access_log; реальная конфигурация сервера не применялась |

Артефакты: `/var/lib/foundryvtt/Data/work-cat/witcher-diagnostics-20260916T224144Z`. Текущий итог — `browser-report.json`; `browser-reports-<runId>` содержит прочитанные отчёты, `browser-local-snapshot.json` и `browser-menu.png` — download/меню. `browser-check.mjs` содержит сценарий и guard мира, `unit-tests.txt` — Node-тесты; `module-hashes.json` фиксирует файлы модуля. Предыдущий неуспешный прогон обнаружил неверное предположение об оболочке skills; исправлено в адаптере, затем выполнен успешный повтор. Контент/механики системы не исправлялись.

Браузерная проверка не имитировала модели данных, но временно подменила доставку новых JS-файлов и проксирование запросов к сборщику. Постоянная регистрация/включение модуля, настоящий путь nginx, браузер пользователя и canvas/WebGL как игровой сценарий не подтверждены. На headless-клиенте отображались предупреждения Foundry о разрешении экрана/аппаратном ускорении; наличие окна не выдано за проверку сцены.

### Параметры подключения и остаток

Для проверки уже подтверждены worldId `witcher-rb-test`, пользователи QA-GM и QA-Player. Их пароли в документацию/код не записывались. Прямое чтение каталога мира от имени агента получило Permission denied; затем для игровых проверок использовался штатный вход браузером, а не обход файлового доступа.

Подготовлены: `127.0.0.1:30121`, HTTPS `/witcher-trpg-diagnostics/`, лог-каталог `/var/lib/foundryvtt/Logs/witcher-trpg-diagnostics`, `/etc/witcher-trpg-diagnostics.env` и отдельный ключ `/etc/witcher-trpg-diagnostics.key`; 7 дней/256 MiB/5000 файлов. Ключ приёма проверяется сервером, а присланные userId/isGM не используются как авторизация. Ключ не доказывает подлинность пользователя Foundry: содержимое остаётся сообщением клиента.

Следующий отдельный шаг — подготовить доступ пользователя foundry к файлам, назначить доступ к журналам/ключу, применить проверенные конфигурации, обеспечить обнаружение модуля Foundry и включить его в тестовом мире. Новые JS-файлы сейчас mode 0660 `rusbar:rusbar`, пользователь foundry не входит в группу rusbar: штатное чтение службой не обеспечено. Запрос `https://foundry.rusbar.site/modules/witcher-trpg-diagnostics/scripts/api.js` дал HTTP 500; модуль отсутствовал в game.modules до временной регистрации в проверочном клиенте. Права/владельцев агент не менял.

Существующий Ansible имеет место для server_directives; пример подготовлен с сохранением отдельного маршрута WoD. Текущий серверный playbook содержит пустой extra при реально настроенном WoD — не применять неполную политику ко всему серверу. Сам Ansible-репозиторий оставлен без изменений.

## Итог

Код модуля и сборщика создан и проверен. TASK-0007 остаётся in-progress до постоянного подключения и завершающей проверки доставки через настоящий HTTPS-маршрут, включая браузер пользователя. TASK-0008 не реализована этой проверкой: использован отдельный приёмочный сценарий модуля.

Официальные опоры: [события Foundry 14](https://foundryvtt.com/api/v14/modules/hookEvents.html), [права документов и пользователей](https://foundryvtt.com/article/users/). Формат данных дополнительно сверялся с установленным ядром и текущими исходниками Witcher.

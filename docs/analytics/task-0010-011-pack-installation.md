# TASK-0010.011 — сборка и установка компедиума травм

2026-09-18, **14.3.1.00070**, ветка dev, HEAD `0537500936de736ce898b0173670c82714b72f23` с рабочими изменениями TASK-0010. Стадия выполнена. Исходные JSON — проверенная [матрица .010](task-0010-010-content-matrix.md); игровой код и JSON в .011 не менялись.

## Установленная база

- Пакет: `TheWitcherTRPG-RB-Version.criticalWounds`.
- Фактический каталог: `/var/lib/foundryvtt/Data/systems/TheWitcherTRPG-RB-Version/packs/criticalWounds`.
- В манифесте сохранён `packs/criticalWounds.db`: Foundry снимает исторический суффикс .db для действующей базы LevelDB.
- Оригинальные файлы прежней базы: `/var/lib/foundryvtt/Data/work-cat/witcher-task0010-011-20260918/backup/criticalWounds`.
- Артефакты сборки/проверок: `/var/lib/foundryvtt/Data/work-cat/witcher-task0010-011-20260918`.

Штатный `@foundryvtt/foundryvtt-cli` **3.0.3**, уже установленный в каталоге инструментов `work-cat/witcher-compendiums-20260916T195048Z/tools/node_modules`, использован напрямую через `compilePack`/`extractPack`. Репозиторный `utils/packs.mjs` вызывает ту же функцию, но перебирает все библиотеки; здесь обработана только criticalWounds. Новые зависимости не устанавливались.

## Порядок и проверка

1. Проверены `ActiveState=inactive`, `SubState=dead`, `MainPID=0` службы foundryvtt и отсутствие процесса Node из /opt/foundryvtt. Служба не останавливалась и не запускалась агентом.
2. Сохранены98 исходных JSON, хеши/метаданные всех7 баз и снимок документации/манифеста. Сборка выполнена в отдельном `build/criticalWounds`, затем извлечена в `readback-built` и полностью сопоставлена по первичным `_key`.
3. Перед установкой повторно подтверждены остановленная служба, неизменность установленной базы относительно снимка и результат сборки. Старые файлы перемещены в backup, новые — в **существующий каталог базы**. Сам каталог сохранил inode, владельца, группу и режим. При отказе операции установки предусмотрен возврат уже перемещённых файлов.
4. SHA256 установленного набора совпал со сборкой; backup сохранил исходные байты и метаданные старых файлов. Остальные6 библиотек побайтово и по метаданным прежние.
5. С установленной закрытой базы снята побайтово идентичная копия `installed-copy`. Извлечение проведено из неё, поскольку открытие ClassicLevel само меняет служебные журналы/manifest. `readback-installed` полностью совпал с актуальными JSON; хеши установленной базы после проверки прежние.

| Проверка | Итог |
| --- | --- |
| Первичные документы | 98: 94 Item и4 папки |
| ActiveEffect | 77;358 строк изменений |
| Новые численные настройки | 343 строки;50 условных;6 исключений производных |
| Ссылки стадий | 93, все цели существуют; совпадают разновидность и место |
| Уникальность травм | Нет повторов woundTypeId/location/treatment внутри пакета |
| Полное структурное равенство | Исходные JSON = snapshot = built readback = installed readback |
| Сохранённые данные | ID/UUID, вложенные effects/changes, flags, длительности, тексты, переходы, заживление и новые настройки совпадают целиком |
| Сторонние базы | combat, lifepath, style, character-generator-sub-tables, character-generator, witcher-lifepath не изменились |

`build.mjs --build` и `build.mjs --installed` выполняют сборку/обратное извлечение и проверки. Результаты: `build-verification.json`, `installed-verification.json`, `installation.json`; первоначальный снимок — `packs-before.json`. Базу `installed-copy` после её открытия не следует сравнивать побайтово с live: факт равенства зафиксирован **до** открытия; после открытия сравниваются документы. Backup не открывался через LevelDB и сохраняет исходный набор.

## Права и границы проверки

Права/владельцы не назначались. Каталог criticalWounds сохранил `foundry:foundry`, режим0770. **Новые файлы созданы инструментом от rusbar:rusbar**, режим0660 (LOCK —0640); пользователь должен привести их права к принятой политике **до запуска Foundry**. В частности, текущая учётная запись foundry не входит в группу rusbar. Это состояние записано в installation.json; автоматического исправления прав нет.

Новая база установлена, но доступ службы, HTTP, импорт в мире, браузерная работа и сохранённые ранее документы Actor этой стадией не проверены. Мир не запускался и не мигрировался. Старые экземпляры травм у Actor не считаются обновлёнными только из-за замены пакета.

Повтор полного набора125 вычислительных тестов .010 не требовался: исходные JSON/код расчёта не изменены, полное равенство сериализованных документов подтверждено. Адресные проверки манифеста/документации/метаданных прошли:98 исходных JSON прежние, в system.json изменена только версия,2673 локальные ссылки доступны; git diff --check без замечаний. Единственный check --freshness графа прошёл: valid=true, current, differences=[];641/5824/15796/488. После записи результатов обновлён только контрольный хеш этого протокола. Статусы issues остаются прежними.

Следующая [.012](../tasks/task-0010-parameter-limits.md#task-0010012) — итоговый статический протокол TASK-0010; после него и команды пользователя — игровая приёмка .013. Для завершения .012 запуск Foundry не требуется.

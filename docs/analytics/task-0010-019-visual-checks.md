# TASK-0010.019 — Визуальная корректировка и синтетические проверки

2026-09-19, `dev`, версия **14.3.1.00082**. Основание — [четыре блока требований пользователя](../tasks/task-0010-parameter-limits.md#task-0010019). Реализация выполнена; игровая приёмка остаётся открытой. Foundry выключена пользователем и в этой проверке не запускалась.

## Изменения и зависимости

| Файлы | Изменение и потребители |
| --- | --- |
| `module/actor/sheets/WitcherCharacterSheet.js` | Базовый размер 1125×800; `_prepareCharacterData` готовит `parameterLabels` из `CONFIG.WITCHER.statMap`; потребитель — `tab-stats.hbs` |
| `module/setup/config.js`, `lang/ru.json`, `lang/en.json` | Восемь `labelFull` и восемь новых строк в каждой локализации. Полные названия производных/максимумов; старые ключи и подписи сохранены |
| `module/actor/sheets/configurations/WitcherModifiersConfiguration.js` | `statRows.label` предпочитает labelFull, затем label, затем прежнюю подпись; пути записи и покупка уровня прежние |
| `templates/partials/character/tab-stats.hbs` | Полные имена через контекст; без него общий шаблон сохраняет model label монстра. Фильтрация состава плиток прежняя |
| `templates/sheets/actor/configuration/app/edit-stats.hbs`, `styles/configurations/modifier-configuration.css` | `modifier-stats`: общая для строк конкретного редактора колонка по самому длинному названию, зазор 10–20px; редактор навыков сохраняет прежнюю сетку |
| `styles/character/sheet.css` | Левая колонка +25%, единые рамки, сетка плиток, прокрутка вкладок, колонки и плюсы инвентаря/магии. Правила ограничены листом персонажа; более специфичный селектор устраняет перекрытие старой общей сеткой Actor |
| `system.json` | Только версия .00082 |

Полные русские подписи сверены с локальным `assets/books/glossary.md` архивной копии: «Устойчивость», «Отдых», «Переносимый вес». Механики, модель данных, списки действий и компедиумы этой правкой не меняются.

## Выполненные проверки

Артефакты находятся вне репозитория: `/var/lib/foundryvtt/Data/work-cat/witcher-visual-019-20260919`. Там сохранены исходный diff/метаданные, запускаемые сценарии, JSON с измерениями и снимки локальных страниц.

| Проверка | Результат | Предел доказательства |
| --- | --- | --- |
| `node --experimental-vm-modules …/layout.mjs` | **60/60**: ru/en × 1366×768/1600×1000/1920×1200, шрифт 100%/125%; три редактора и семь вкладок. Равные плитки и рамки, `+99` без обрезки, названия/зазоры колонок, плюсы, заголовки магии в строку, постоянная высота | Настоящие HBS, helpers, модель/подготовка контекста и CSS системы/ядра; оболочка приложения и переключение вкладок смоделированы. Нет нативного ApplicationV2 lifecycle или игрового сохранения |
| `node --experimental-vm-modules …/populated.mjs` | **8/8**: ru/en × два масштаба × инвентарь/магия. Оружие, броня, ценности, контейнер, заклинания; колонки не пересекаются, кнопки выровнены, рамки и полоса нагрузки сохраняют высоту при растягивании | Предметы — локальные фикстуры контекста. Клики игровых обработчиков не выполнялись |
| `node --experimental-vm-modules --test …/witcher-task0010-012-20260918/form.test.mjs` | **4/4**: фильтрация сохраняемых полей, customStat/навык, доступность кнопок, передача параметра обработчику повышения | Настоящие методы/шаблоны, запись в БД подменена существующим стендом |
| `node --experimental-vm-modules …/shared-template.mjs` | **4/4**: ru/en × персонаж/монстр; полные имена персонажа, все подписи монстра совпадают с исходным шаблоном | Настоящие модели/шаблон; нативное окно монстра не запускалось |
| JS/JSON/локализация | Синтаксис трёх JS проверен, JSON читаются; существующие ru/en ключи и значения сохранены, добавлено по восемь строк | Без загрузки ресурсов службой Foundry |
| Документация/граф | Актуализированы 10 карточек/источников, добавлены 17 сущностей и45 связей, уточнены два процесса и адреса строк. `query.py check --freshness --format json`: valid/current, 641 источник; адресная проверка потребителей parameterLabels прошла | Полная регрессия справочника не запускалась: поисковый движок/формат не менялись |

При локальной проверке выявлены и исправлены перекрытие sidebar старым селектором, влияние отступов `.scrollable` ядра на ширину рамок и разница высот заголовков с опциями/без них. Итоговые результаты выше относятся к исправленной версии. Node сообщает стандартные предупреждения существующего стенда о VM modules/типе пакета; ошибок проверок нет.

Перекрёстная проверка: все прежние ru/en ключи и значения сохранены; diff не содержит изменений механик/пакетов. Проверены 3736 локальных ссылок в затронутых Markdown. Метаданные доступа и inode всех 2103 отслеживаемых файлов совпали с исходным снимком. `git diff --check` прошёл.

## После запуска Foundry пользователем

1. Перезагрузить клиент, открыть нового и заполненного персонажа; проверить реальную базовую геометрию и сохранение пользовательского растягивания при переключении семи вкладок.
2. Проверить прокрутку левой панели/вкладок, полные названия, репутацию и доступность всех строк на экране пользователя.
3. Открыть оба редактора, изменить значение, повторно открыть; проверить прежние действия повышения и соседний редактор навыков.
4. Проверить пустые и заполненные инвентарь/магию: сворачивание секций, добавление, опции существующих предметов, отсутствие обрезки заголовков и скачков при растягивании.
5. Продолжить оставшуюся [игровую матрицу .013](task-0010-013-browser-checks.md), получить визуальную приёмку пользователя. Только синтетические результаты не закрывают задачу или issues.

# module/activeEffect/effectExpiry.js

## Текущее состояние — 14.3.1.00068

2026-09-18, TASK-0010.009. **Назначение:** Итоговый пересчёт после пакетного истечения.

**Методы, сущности, действия и зависимости:** registerEffectExpiry один раз оборачивает refresh существующего foundry.documents.ActiveEffect.registry, сохраняя this, аргументы и Promise. На активном GM собирает уникальных Actor с учётом context.actors и открывает withParameterChanges вокруг всего native refresh/modifyBatch. DryRun и реальные ответы завершаются до нормализации; остальные клиенты лишь обновляют native часы. Ядро по-прежнему выбирает срок/expiryAction и выполняет запись. Ошибки и частично сохранённое состояние проходят через finally существующего параметрического обработчика; новый механизм блокировок/rollback не добавлен.

[Проверки и границы](../../../../task-0010-009-checks.md). Реальная игровая приёмка предстоит в .013.

# module/setup/deprecations.js

| Поле | Значение |
| --- | --- |
| Исходный файл | [module/setup/deprecations.js](../../../../../../module/setup/deprecations.js) |
| Тип файла | JavaScript — пустой обработчик |
| Статус анализа | Проверено |
| Дата проверки | 2026-09-10 |
| Ветка и коммит | `rusbar-main`, `3252300787c348e11f95098c345a6af7704b690c`; исходник совпадает со срезом TASK-0001 `15da5b225535e34af4e132c701b5353ef4eb667f` |
| Изменения относительно коммита | Нет |
| Задача и порция | [TASK-0002](../../../../../tasks/task-0002-system-initialization.md); порция 4 |
| Запись перекрёстной сверки | [Журнал сверок](../../../review-log.md) — TASK-0002, порция 4 |

## Назначение файла

Экспортирует пустую функцию уведомлений deprecationWarnings; проверки устаревания не выполняет.

## Условия использования

[module/TheWitcherTRPG.js](../../../../../../module/TheWitcherTRPG.js) импортирует функцию и вызывает в конце ready. При импорте файла читается foundry.applications.api.DialogV2.

## Введённые сущности и действия с ними

| Сущность | Определение | Действие |
| --- | --- | --- |
| DialogV2 | Локальная const, стр. 1 | Сохраняет ссылку на API; далее не используется |
| deprecationWarnings | Экспорт const function, стр. 3 | Пустое тело |

## Основные функции и методы

deprecationWarnings() не принимает аргументов, синхронно возвращает undefined. Никаких ветвлений, проверок версий, диалогов или уведомлений в теле нет.

## Используемые сущности и зависимости

| Сущность | Источник | Вид связи | Доказательство |
| --- | --- | --- | --- |
| DialogV2 | Foundry: foundry.applications.api.DialogV2 | Чтение глобального API при импорте | Стр. 1; вызовов методов DialogV2 в файле нет |

## Известные потребители

[module/TheWitcherTRPG.js](../../../../../../module/TheWitcherTRPG.js) — импорт и вызов deprecationWarnings в ready (стр. 91). В области module других потребителей не найдено.

## Данные и изменения состояния

Только локальная ссылка при импорте. Функция не читает версии, не записывает документы/настройки и не изменяет интерфейс.

## Проверки и доказательства

Полностью прочитаны 7 строк. Проверены импорт и вызов из точки входа; содержимое пустого тела устанавливается непосредственно по исходнику. Отдельный запуск пустой функции для этого вывода не требуется.

## Непроверенные участки и открытые вопросы

Название файла не определяет обещанного поведения: отсутствующая реализация сама по себе здесь не классифицирована как ошибка. Уведомления, реализованные другими частями системы/ядром, не исследовались целиком.

## Связанные проблемы

Отдельной проблемы не зарегистрировано. Условие достижения последнего вызова ready описано в [issue-00002](../../../../../issues/potential/issue-00002.md).

## История актуализации

2026-09-10 — первичный разбор полного файла на указанном коммите; сверка порции 4 отражена в журнале. Файлы зависимостей проверены в пределах определений и обращений, без объявления их полного разбора.

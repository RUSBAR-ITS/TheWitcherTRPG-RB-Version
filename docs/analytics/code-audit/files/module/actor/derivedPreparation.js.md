# module/actor/derivedPreparation.js

## Текущее состояние — 14.3.1.00063

2026-09-18, TASK-0010.005. **Назначение:** Расчёт производных с исключениями по строкам эффекта.

**Методы, сущности, действия и зависимости:** derivedStatInput перечитывает input и контекст primary из parameterPreparation, фильтрует excludedDerived для всех рёбер пути и вызывает parameterCalculation.calculateParameter. calculateDerivedParameter получает формулу/ручную базу из derivedStatData, рекурсивно передаёт путь в Run, применяет собственные строки после базы и записывает результат только в transient actor.parameterModifiers. Исключение собственной строки проверяется по потребителям, не по самой цели. uninjured исключает контекстные штрафы для порога/весовой ёмкости; порог всегда использует эту ветвь. nativeOffset сохраняет разницу max/base, оставленную нативным initial. Нет Actor.update и смены phases.

[Исходник](../../../../../../module/actor/derivedPreparation.js), [проверки и границы](../../../../task-0010-005-checks.md). Браузерная приёмка отложена. Ниже, если есть, сохранены датированные предыдущие срезы; изменённые расчёты описаны здесь.

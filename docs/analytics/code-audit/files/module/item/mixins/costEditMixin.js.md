# module/item/mixins/costEditMixin.js

| Поле | Значение |
| --- | --- |
| Исходный файл | [module/item/mixins/costEditMixin.js](../../../../../../../module/item/mixins/costEditMixin.js) |
| Тип файла | JavaScript, ES module |
| Статус анализа | Проверено |
| Дата проверки | 2026-09-10 |
| Ветка и коммит | `rusbar-main`, `c7cd9d71dcb1714cdccb175aee351a3f1df95c5b` |
| Изменения относительно коммита | Нет; содержимое также совпадает со срезом TASK-0001 `15da5b225535e34af4e132c701b5353ef4eb667f`. |
| Задача и порция | [TASK-0003.017](../../../../../../tasks/task-0003.017.md), одна порция из пяти файлов |
| Запись перекрёстной сверки | [TASK-0003.017](../../../../review-log.md#task-0003017) |

## Назначение файла

Слушает изменения цен компонентов и пересчитывает сумму дополнительных расходов в DOM. Сумму передаёт callback владельца примеси.

## Условия использования

Именованный export costEditMixin. Единственный найденный потребитель — RepairSystem: добавляет методы к Repair.prototype и из render callback DialogV2 вызывает attachHtmlListeners. Обращения выполняются к глобальному document, корень окна не передаётся. От импорта DOM не меняется.

## Введённые сущности и действия с ними

| Сущность | Вид и место определения | Назначение | Доступность или регистрация | Действия и жизненный цикл |
| --- | --- | --- | --- | --- |
| costEditMixin | Экспорт let,1–20 | Два метода стоимости | Object.assign в repair.js:343 | Без собственных полей/регистрации Foundry |
| change listener | Стрелочная функция,4 | Пересчитать сумму при изменении поля | el.addEventListener для каждого component-cost | Замыкает callback,args,this; ссылка для снятия не сохраняется |

## Основные функции и методы

| Функция или метод | Входы и предусловия | Результат | Основные действия | Ошибки, асинхронность и изменения состояния |
| --- | --- | --- | --- | --- |
| attachHtmlListeners(callback,...args):2–5 | Глобальный document и вызывающий объект с _calculateAdditionalCost | undefined | Находит все component-cost; каждому добавляет change callback(this._calculateAdditionalCost(),...args) | Не ограничивает поиск окном и не снимает старые обработчики; повторная подписка добавляет listeners |
| _calculateAdditionalCost():7–19 | Все component-cost.value и первый #total-price с data-price | number, включая NaN | reduce parseInt(value)??0; parseInt(data-price)+additionalCost; присваивает innerText; возвращает additionalCost | Пустое value→NaN; дроби parseInt обрезает; нет total-price→TypeError. Никаких Item.update/денежных операций |

## Используемые сущности и зависимости

| Используемая сущность | Файл-источник или внешний API | Вид связи | Где и зачем используется | Основание |
| --- | --- | --- | --- | --- |
| document.getElementsByClassName/getElementById, addEventListener, innerText | Web DOM API | Глобальное чтение/запись | 2–18; не использует element конкретного Dialog | Настоящий метод исполнен с минимальным DOM-фасадом; браузер не запускался |
| parseInt / ?? / Array.from / reduce | JavaScript (Node 24.16.0 в проверке) | Преобразование и сумма | 9–14 | NaN не nullish; целочисленный разбор подтверждён |
| component-cost, total-price, data-price | [templates/partials/components-list.hbs](../../../../../../../templates/partials/components-list.hbs) | DOM-контракт | Поля стоимости и сохранённая базовая сумма | Шаблон задаёт class/input без value и id/data-price span |
| additionalCost / render callback | [module/item/systems/repair.js](../../../../../../../module/item/systems/repair.js) | Вызов/получатель результата | renderDialog:114, RepairData.repairPrice | callback присваивает data.additionalCost; getter прибавляет цены owned/missing |

## Известные потребители

| Файл-потребитель | Используемая сущность этого файла | Способ и условия использования | Основание |
| --- | --- | --- | --- |
| [module/item/systems/repair.js](../../../../../../../module/item/systems/repair.js) | costEditMixin | Import4; Object.assign343; render114 | Единственный импорт в module/ |

Область поиска: текущие module/ и templates/; регистрации сверены отдельно. Динамические обращения внешних модулей не исследовались.

## Данные и изменения состояния

Изменяет текст только первого найденного total-price и вызывает переданный callback. additionalCost хранится в RepairData по решению потребителя, сам mixin не знает Actor/Item. data-price остаётся исходной базой; повторный пересчёт складывает с ней текущую сумму, а не с прежним innerText. Нет проверки конечности чисел или неотрицательности; допустимые деньги/округление требуют отдельного решения.

## Проверки и доказательства

| Что проверено | Источник, команда или сценарий | Фактический результат | Ограничения |
| --- | --- | --- | --- |
| Нормальная сумма | Настоящий метод; input2/3, data-price15 | Возвращены 5, innerText20 | DOM-фасад |
| Пустое поле | Один input очищен | additionalCost и total стали NaN | Ранее issue-00100; повторный ID не создан |
| Два контекста | attach A при inputA2; добавить inputB3, attach B; change на A | listeners[2,1], оба callback получили 5, первый total20, второй остался 40 | Условное сосуществование DOM двух окон; реальный modal lifecycle не проверен |
| Полный файл/связи | 20 строк, оба метода; rg по module/templates | Примесь используется только ремонтом, partial формирует нужные селекторы | Чтение не подтверждает штатный UI во всех состояниях |

## Непроверенные участки и открытые вопросы

В TASK-0004.007 текущие определения и потребители сопоставлены; прежние опыты TASK-0003.015/.016/.017 (2026-09-10) и .034 (2026-09-11) сохраняют собственные входы и фасады. Браузер, мир, сеть и запись в БД не запускались. Точные оставшиеся вопросы и ответственные блоки: [U007-01](../../../../cross-check-0002.md#u007-01), [U007-02](../../../../cross-check-0002.md#u007-02), [U007-08](../../../../cross-check-0002.md#u007-08). Для этого файла установлены процессы R007-15, а не полный клиентский lifecycle.

## Связанные проблемы

[issue-00100](../../../../../../issues/potential/issue-00100.md), [issue-00106](../../../../../../issues/potential/issue-00106.md). 100 — NaN пустой цены;106 — условное смешивание двух диалоговых контекстов. Округление/диапазон цены описаны как контракт к дальнейшему согласованию, не как новые правила.

## История актуализации

2026-09-10 — полный разбор файла и сверка определений, потребителей и внешнего API на указанной версии. Результаты приведены в записи TASK-0003.017 журнала. Проверка описания не означает проверки мира или отсутствия ошибок.

## Сквозная сверка TASK-0004.007

2026-09-14; rusbar-main, 6a26042f7881d9990c304483c7219e0698f6617e. Исходник совпадает со срезом TASK-0001; изменено только описание.

Стоимость читается глобально из .component-cost, итог — из первого #total-price с data-price. ParseInt пустого поля даёт NaN, который ??0 не заменяет. Повторный listener/два DOM-набора могут смешивать сумму; такой DOM-опыт не доказывает достижимость двух окон. RepairData отображает дополнительную цену и не списывает деньги.

Сопоставленные определения и потребители: [templates/partials/components-list.hbs](../../../templates/partials/components-list.hbs.md), [module/item/systems/repair.js](../systems/repair.js.md).

[Протокол и границы](../../../../review-log.md#task-0004007) — TASK-0004.007; процессы [R007-15](../../../../cross-check-0002.md#r007-15). В этой порции выполнена статическая сверка; прежние опыты сохраняют свои даты и фасады. Единственный новый запуск N007-01 проверяет producer/HBS alchemyComponentsList на заданном контексте; его границы не распространяются на остальные процессы.

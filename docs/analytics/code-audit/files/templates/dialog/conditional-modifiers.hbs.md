# templates/dialog/conditional-modifiers.hbs

## Текущее состояние

**14.3.1.00065, TASK-0010.007.** Получает candidates из conditionalModifiers.chooseRollModifiers, выводит безопасно экранированные подписи/цель/локализованную native операцию/число. checkbox modifier-index не имеет checked; индекс используется только внутри одного диалога для выбора стабильного ID. Сам шаблон не меняет AE/Actor.

[Исходник](../../../../../../templates/dialog/conditional-modifiers.hbs); [проверка и пределы](../../../../task-0010-007-checks.md).

## Назначение и использование

Список условных строк без сохранённого выбора. Получает candidates из conditionalModifiers.chooseRollModifiers, выводит безопасно экранированные подписи/цель/локализованную native операцию/число. checkbox modifier-index не имеет checked; индекс используется только внутри одного диалога для выбора стабильного ID. Сам шаблон не меняет AE/Actor.

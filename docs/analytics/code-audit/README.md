# Пофайловое исследование системы

Исследование описывает текущие файлы системы, введённые ими сущности и связи между файлами. Основа подготовлена по [TASK-0001](../../tasks/task-0001-code-inventory.md); в [TASK-0002](../../tasks/task-0002-system-initialization.md) разобраны манифест, точка входа и регистрация компонентов — 11 файлов. [TASK-0003.001](../../tasks/task-0003.001.md) добавила пять карточек общих полей и характеристик.

| Материал | Назначение |
| --- | --- |
| [Реестр файлов](registry.md) | Одна строка на каждый включённый файл, назначение, ссылка на карточку и статус |
| [Шаблон карточки](templates/file.md) | Единый состав описания файла и таблиц связей |
| [Карточки файлов](files/README.md) | Описания с повторением путей исходников |
| [Журнал сверок](review-log.md) | Проверки состава, согласованности описаний и применимости выводов |

## Исследуемая версия

| Поле | Значение |
| --- | --- |
| Дата инвентаризации | 2026-09-10 |
| Ветка | `rusbar-main` |
| Коммит | `15da5b225535e34af4e132c701b5353ef4eb667f` |
| Каталог checkout | `/var/lib/foundryvtt/Data/systems/TheWitcherTRPG-RB-Version` |
| Рабочее дерево на старте | Чистое; существенных для исследования незакоммиченных изменений нет |
| Отслеживаемые файлы исходного среза | 706 |
| Исключённые отслеживаемые файлы | 85 |
| Включённые файлы | 621 |
| Дополнительные неотслеживаемые или игнорируемые файлы в границах анализа | 0 |
| Карточки с завершённым пофайловым разбором после TASK-0001 | 0 |

По сравнению с оценкой при постановке задачи (701 файл) добавились пять файлов задач в `docs/`. Они исключены: отслеживаемых файлов стало 706, исключённых — 85, состав исследования остался равен 621 файлу.

Все 621 файла существуют в рабочем каталоге; их содержимое на момент инвентаризации совпало с Git-объектами указанного коммита. Наличие файлов и совпадение содержимого не подтверждают правильность их поведения. Чтение байтов для контрольных сумм не считается пофайловым разбором.

## Границы и состав

Текущее покрытие после TASK-0003.001: **16 карточек проверены, 605 файлов не разобраны**. На `7b7788bc614e5b7a57f8c596fb64ca75ecabd8b7` все 621 исходник вновь сопоставлены со срезом TASK-0001 без расхождений; на старте текущей порции отслеживался 741 файл.

После TASK-0002 были проверены 11 карточек, не разобраны 610 файлов. Проверка выполнена 2026-09-10 на `3252300787c348e11f95098c345a6af7704b690c`; состав и содержимое 621 исходника совпали с базовым срезом TASK-0001. Исторические количества в таблице инвентаризации выше сохранены. В HEAD проверки TASK-0002 отслеживалось 711 файлов; рост относится к исключённой документации.

В [журнале](review-log.md) сохранены четыре порции TASK-0002 и её итоговая сверка; добавлена [сверка TASK-0003.001](review-log.md#task-0003001). TASK-0002 дала девять новых проблем, TASK-0003.001 — ещё четыре (`issue-00011`–`issue-00014`). Вместе с исходной issue-00001 в [реестре проблем](../../issues/README.md) 14 карточек, все в `potential`.

В TASK-0003.001 использованы настоящие классы данных Foundry 14.367.0 в Node. Проверки TextEditor, двух методов Actor и передачи данных шаблона отделены от них, поскольку использовали подмены соседних компонентов. Мир и браузер не запускались.

| Каталог или группа | Файлов в исследуемом срезе |
| --- | --- |
| Корень: `build.json`, `package.json`, `system.json` | 3 |
| `lang/` | 8 |
| `module/` | 214 |
| `packsJson/` | 226 |
| CSS из `styles/` | 36 |
| `templates/` | 132 |
| `utils/` | 2 |
| **Всего** | **621** |

Включены программный код, шаблоны, CSS, локализации, данные компедиумов, инструменты, манифест и конфигурация сборки. Список получен сопоставлением Git и фактического дерева; он не ограничивается файлами, которые удалось найти обычным поиском с учётом `.gitignore`.

Новые файлы вне перечисленных исключений нельзя исключать автоматически из-за расширения или расположения. При появлении такого файла установить его роль и согласовать изменение границ, если оно необходимо.

## Согласованные исключения

Пользователь согласовал перечень 2026-09-10. Все пути указаны относительно корня репозитория. Каталоги исключаются целиком, включая вложенные файлы. Шаблон `packs/**/LOCK` исключает только файлы с именем `LOCK` внутри `packs/`.

| Путь | Причина исключения |
| --- | --- |
| `docs/` | Вся документация проекта, включая результаты этого исследования |
| `assets/` | Изображения; в проверенном срезе 36 PNG и 3 SVG |
| `.github/` | Шаблоны GitHub и автоматизация |
| `.git/` | Служебные данные Git |
| `README.md` | Вводная документация |
| `AGENTS.md` | Инструкции работы с репозиторием |
| `LICENSE` | Текст лицензии |
| `.gitignore` | Правила исключения файлов из Git |
| `.prettierrc` | Настройки форматирования |
| `.prettierignore` | Исключения для форматирования |
| `jsconfig.json.default` | Шаблон конфигурации среды разработки |
| `package-lock.json` | Автоматически сформированный перечень зафиксированных npm-зависимостей |
| `styles/fonts/thewitcher2.ttf` | Файл шрифта |
| `packs/**/LOCK` | Служебные файлы блокировки; сейчас пять пустых файлов |

Исключённые файлы не получают строк реестра и отдельных карточек. Они могут использоваться как справочные источники: `AGENTS.md` продолжает определять правила работы, а `package-lock.json` при необходимости помогает уточнить версии зависимостей. Ссылки из кода на исключённые ресурсы фиксируются в карточке использующего их файла с пометкой «вне границ пофайлового анализа».

`packsJson/` остаётся в исследовании. Для `packs/` исключены только файлы `LOCK`; весь каталог не исключён. Ссылка на исключённый файл не включает его обратно в реестр.

Вся папка `docs/` исключена, хотя именно в ней хранятся результаты исследования. Дополнительные исключения требуют согласования пользователя.

## Реестр и карточки

В реестре используется полный путь относительно корня системы. Одна строка соответствует одному исходному файлу; сортировка — по пути. Подробное описание хранится под `files/` с сохранением пути и расширения исходника и добавлением `.md`.

Пример: `module/TheWitcherTRPG.js` → `files/module/TheWitcherTRPG.js.md`. Исходные имена сохраняются, включая регистр: это обеспечивает однозначное соответствие файлов и карточек. `files/README.md` — указатель каталога, а не карточка исходного файла.

До чтения файла назначение в реестре отмечается как «Не установлено», описание — «Не подготовлено». Ссылка на карточку появляется только после создания файла описания. Совпадение имён или каталогов само по себе не служит основанием для описания назначения.

| Статус анализа | Значение |
| --- | --- |
| Не начат | Файл включён в реестр; пофайлового разбора ещё нет |
| В работе | Разбор частичный; в карточке явно перечислены ещё не исследованные участки |
| Проверено | Согласованный объём разбора завершён, карточка сверена с исходником и связями; ограничения проверки указаны |
| Требует актуализации | Изменился исходник или связанная сущность, влияющая на выводы; описание нужно пересмотреть |

Статус «Проверено» относится к описанию на указанной версии. Он не означает отсутствие ошибок в системе или проверку её запуска. Эти статусы отделены от статусов задач и проблем.

## Состав описания и связей

Каждая карточка следует [шаблону](templates/file.md) и содержит:

- Назначение файла и условия его участия в работе системы.
- Введённые сущности: классы, функции, модели, поля, настройки, константы, обработчики и другие определения.
- Основные функции и методы с существенными входами, результатами и действиями с данными.
- Используемые сущности из других файлов и внешних API, места и причины их использования.
- Известных потребителей, условия вызова и существенные изменения состояния.
- Источники доказательств, фактически выполненные проверки, непроверенные вопросы и ссылки на проблемы.

Связь записывается в едином формате: используемая сущность, файл-источник или внешний API, вид связи, место и цель использования, основание вывода. Имя сущности указывается так, как в коде; при переименовании импорта фиксируются исходное имя и локальный псевдоним. Для различения одноимённых сущностей используются путь файла и полное имя, например класс и метод.

Учитываются импорты, вызовы, наследование, регистрация классов и моделей, `Hooks`, обращения к глобальным объектам и настройкам, шаблонам, локализации и ресурсам. Регистрация обработчика, возможный вызов и выполненный при запуске вызов описываются раздельно. Неустановленный источник или динамическое разрешение помечаются явно.

Для шаблонов указываются входной контекст, используемые помощники, включаемые шаблоны и события интерфейса; для JSON — структура данных, значимые ключи, ссылки и потребители; для CSS — селекторы, подключения и связанные элементы интерфейса. Отсутствие функций у неисполняемого файла фиксируется прямо.

## Работа порциями и перекрёстная сверка

1. Перед порцией проверить коммит, рабочее дерево и применимость исследуемого среза.
2. Читать файлы порции и создавать карточки; обычно порция содержит 5–10 связанных файлов, крупный файл разбирается отдельно или по разделам.
3. Проверять определения используемых сущностей и их потребителей. Просмотр отдельного определения в соседнем файле не означает завершения его пофайлового разбора.
4. Сопоставлять карточки с кодом и между собой: исходящая связь должна согласовываться с описанием определения и известного потребителя, если эти файлы уже разобраны.
5. Проверять охват порции, пути, ссылки и статусы; записывать результаты и ограничения в [журнале](review-log.md).
6. Обновлять реестр, относящиеся к порции задачи и CHANGELOG по фактическому результату.

Граф зависимостей и полные описания процессов будут строиться позднее. Сейчас собираются проверяемые сведения для них.

## Обновление исследуемой версии

Коммит в этой основе фиксирует исходный срез. Перед продолжением сравнить его с фактически исследуемыми файлами; незакоммиченные изменения указать отдельно. Для файла вне коммита сохранить дату и контрольную сумму содержимого.

Если исходник изменился, пересмотреть карточку и зависящие от изменения выводы, отметить необходимость актуализации. В карточке записать версию, на которой проверено её содержимое; не подменять этот реквизит текущим HEAD без проверки. Обновление карточки сопровождается новой записью сверки, а не удалением свидетельства о ранее выполненной проверке.

При изменении состава перечислить добавленные, удалённые или переименованные файлы, пересмотреть реестр и ссылки, объяснить отличия от исходного среза. Изменения только внутри исключённых путей не добавляют строк в реестр. Перед сменой общего исследуемого среза согласовать её с пользователем.

## Проблемы и пределы исследования

Найденные проблемы регистрируются в `docs/issues/potential` по [правилам](../../rules/issues.md): пользователь разрешил регистрацию в ходе этого исследования. Перед новой карточкой проверяется [реестр проблем](../../issues/README.md). Подтверждение проблемы и исправления остаются отдельными согласованиями.

Инвентаризация проверяет состав файлов и состояние исходников. Она не подтверждает загрузку мира, работу интерфейса, игровые вычисления, полноту связей или отсутствие ошибок. В TASK-0001 новые проблемы системы при сверке состава не выявлены.

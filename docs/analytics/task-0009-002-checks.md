# TASK-0009.002 — модель и редактор, проверки .00048

Дата: 2026-09-17; dev; база 85d8d65ecb541d83d1ea5fa11c69824cb0d125a4. [Проект](../tasks/task-0009-critical-wound-behavior.md#task-0009002).

## Область реализации

Новые schema/defaults/ленивый срок, проверяемые формулы, специализированная конфигурация, два адресных UUID Drop, ru/en и отображение неопределённого срока. Удалены два старых calculateHealingTime, поле followUp и сохраняемый healingTime. Старые ключи словарей сохранены; JSON/базы компедиумов не изменены.

Для согласованности адаптированы непосредственно зависимые потребители: запреты heal/treat, guard неопределённого срока, treatedWound вместо followUp, проверка получателя и ожидание create/delete. Это промежуточное поведение: новая стадия пока создаётся отдельным Item, нет очереди/повторов/кнопки стабилизации; heal пока содержит treated/deadly gates и вызывает treat. Полный lifecycle будет реализован в .003–.005. Старый компедиум не является приёмкой новой модели: новые запреты по умолчанию включены, заживление выключено до пересоздания данных .008.

## Изолированные проверки

Артефакты: /var/lib/foundryvtt/Data/work-cat/witcher-task0009-002-20260917/.

Команда node --test model-editor.test.mjs: **10/10**. Настоящие Foundry TypeDataModel, поля, Roll/грамматика и Handlebars; документ-владелец — локальная DataModel, лист/события/UUID lookup/запись — фасады. Проверены defaults и сериализация, книжные сроки по BODY.value, константы/дроби/функции, запрет кубов/неизвестных переменных, конечный положительный результат, динамический getter без записи, проверки семьи/места/self/access, адресный Drop и текстовый submit, отсутствие операций при выключенном заживлении/лечении и условный вывод кнопки.

Первый запуск: 7/10; три отказа вызваны неполным bootstrap Foundry (CONFIG.Dice.functions и TYPES родительской модели). Фасады дополнены по исходникам ядра; ожидания результатов не менялись. Итог: 10/10.

Это частичное покрытие С01/С02/С10. Обязательный ID на Actor (.004), две кнопки/операции (.003), завершение дня (.005), серверная сериализация, настоящий Drop/reload и Б01/Б02/Б13 не проверены. Игровая приёмка отложена до команды пользователя. Статусы issues не меняются.

## Справочник и оформление

Карточки отражают промежуточный код. Удалённые декларации выведены из активного графа с сохранением ID в retired_ids; новые методы и потребители добавлены адресно. Будущие операции не объявлены реализованными. Проверки завершены: 10/10 изолированных сценариев настоящей модели/Roll, node --check для пяти JS, компиляция трёх HBS; по 20 новых ключей ru/en с сохранением прежних значений. Пять адресных тестовых методов справочника, включая 32 CLI-сценария, прошли за 139.071 с. Проверка графа valid=true/current: 624 источника, 5589 сущностей, 15646 отношений, 469 процессов, 896 контролируемых документов и Foundry 14.367.0 без расхождений. Проверены 7304 локальные ссылки, включая 876 якорей; ошибок нет. Метаданные доступа исходных файлов сохранены, изменений вне согласованных исходников/документации нет; JSON/базы packs и статусы 264 potential / 0 open / 70 closed прежние. Полный набор тестов справочника и браузер не запускались.


Первый check справочника выявил ошибку обновления included: локальные boundary были ошибочно посчитаны определениями. Список исправлен согласно формату, сохранены прежние состояния незатронутых источников; зависимые проверки повторены успешно. Первый адресный unittest завершился в setup по той же причине, итоговый прогон всех пяти выбранных методов — OK.

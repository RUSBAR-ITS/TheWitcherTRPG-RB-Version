# Состояние репозитория

Дата проверки: **2026-09-10**. Источники: текущие Git-ссылки, файлы манифеста и инструментов этого checkout.

## Ветки и каталог

| Поле | Значение |
| --- | --- |
| Каталог | `/var/lib/foundryvtt/Data/systems/TheWitcherTRPG-RB-Version` |
| Рабочая ветка | `rusbar-main` |
| HEAD | `059f0c775dc47914dcad4b50167c934ad2f44929` |
| Локальная ссылка `origin/rusbar-main` | `059f0c775dc47914dcad4b50167c934ad2f44929` |
| Локальная ссылка `origin/original` | `2b247fa59b8a09757a98ba36918459061af78137` |
| Локальная ссылка `origin/HEAD` | `origin/rusbar-main` |

Пользователь назначил `rusbar-main` основной веткой. `original` предназначена для сравнения и восстановления исходной версии. Указанные ссылки проверены локально; настройки защиты веток на GitHub не проверялись.

## Манифест

В [system.json](../../system.json) указаны:

- `id: "TheWitcherTRPG"`;
- название `The Witcher TRPG - Foundry VTT`;
- минимальное, проверенное и максимальное поколение Foundry — `14`;
- заготовки версии и адресов выпуска, подставляемые release workflow.

## Наблюдение: возможное несоответствие регистрации в Foundry

Наблюдение зарегистрировано в [issue-00001](../issues/potential/issue-00001.md). В карточке приведены факты, предполагаемое влияние и пределы проверки.

Статус — `potential`: пользователь согласовал регистрацию, но не подтвердил наличие проблемы. Задача не создана; приоритет и техническое решение не определены.

## Доступные команды

В [package.json](../../package.json) определены:

| Команда | Назначение |
| --- | --- |
| `npm run build:db` | Сборка packs через `utils/packs.mjs` |
| `npm run extract` | Извлечение packs в JSON через `utils/extract.mjs` |

Команда `npm test` в текущем package не определена. Перечисленные команды сборки и извлечения записывают данные; при подготовке этого документа они не запускались.

## Пределы проверки

Проверены локальные Git-ссылки, поля манифеста и scripts в package. Документ не подтверждает запуск системы в браузере, отсутствие конфликта, корректность выпуска или состояние игровых данных.

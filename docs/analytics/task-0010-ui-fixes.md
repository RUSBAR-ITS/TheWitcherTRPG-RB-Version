# TASK-0010.014–.018 — Исправления интерфейса персонажа

**Принято и закрыто 2026-09-19, 14.3.1.00084:** пользователь подтвердил завершение [TASK-0010/.001–.019 и 27 полностью решённых issues](../tasks/task-0010-parameter-limits.md#closure). Частичная 00028 и отдельные TASK-0011/0012/0013 не закрыты. Ниже сохранён датированный протокол с фактическими версиями, охватом и ограничениями; прежние ожидания подтверждения завершены этим решением.

**Актуальный повтор 2026-09-19:** [игровая проверка и регрессия .00082](task-0010-regression-2026-09-19.md) выполнены; новых дефектов в перечисленном охвате не найдено. Предыдущие блокировки мастера/Item.skill сняты. Закрытие и визуальное принятие подтверждены пользователем в .00084. Ниже сохранён датированный прежний протокол; его отметки «ожидается» описывают тот срез.

Дата: 2026-09-19. Срез: `dev`, HEAD `60475d6d7cd7d035a8cc8d89ddfc1c069b83b5a3` плюс согласованные изменения рабочего дерева, версия **14.3.1.00077**. [План и критерии UI01–UI12](../tasks/task-0010-parameter-limits.md#ui-fixes).

## Реализация

| Подзадача | Выполнено | Основная граница |
| --- | --- | --- |
| .014 / 00015 | Skill._initialize берёт ключ имени из собственного EmbeddedDataField.schema.label. Статическая схема не пытается получать label аргументом defineSchema. Настройка видимости монстра передаёт label строки явно. | Семь моделей групп → Skill → карточка/редактор/настройка монстра; источник значений и флагов прежний. |
| .015 | Одна строка на параметр/навык: полное имя, база, итог, базовый предел, повышение. Для характеристик «Повысить параметр». Подробности навыка раскрываются отдельно. Редактор 820×620 с ограничением экраном, внутренним скроллом и переносом действий на узком окне. Рендерится только соответствующая типу часть. | _prepareContext → HBS/CSS → прежние _processFormData, statMixin, skillMixin. Покупка и allow-list не переписаны. |
| .016 | _prepareCharacterData готовит шесть позиций headerSummary, прочерки для пустоты/возраста0, приоритет заполненного Item.homeland перед Actor. hasItemHomeland/homelandLabel используются и в предыстории. Оба списка получили пустой вариант «Не указано». | Только представление; готовые подписи/прочерки в Actor не записываются. |
| .017 | Полоса и подписи внутри carry-meter, накладываются одной ячейкой Grid. Убрано растягивание строки нагрузки; группы инвентаря не раздуваются при увеличении высоты окна. | totalWeight/enc и формулы прежние. |
| .018 | Шкала 12/14/18/24/32 через переменные Foundry; рабочий Arial с кириллицей, декоративное имя Georgia. Селекторы ограничены witcher-character и modifier-configuration. | Визуальная оценка пользователя ещё ожидается. |

В ru/en добавлены 12 одинаковых ключей `WITCHER.Editor`; прежние ключи сохранены. Другие языки, компедиумы, эффекты и механики расчётов не менялись. Для монстра затронута только подпись предусмотренных строк настройки видимости; отдельная issue-00018 и глобальный jQuery из00167 этой порцией не исправляются.

## Локальные проверки

Артефакты и запускаемые сценарии: `/var/lib/foundryvtt/Data/work-cat/witcher-character-ui-fixes-20260919`.

| Проверка | Результат | Что подтверждено / ограничения |
| --- | --- | --- |
| `node --experimental-vm-modules --test labels.test.mjs` | 7/7 | Настоящие CharacterData/MonsterData и Foundry DataModel: 52 имени с первого создания и после roundtrip/reset; 15 несовпадающих названий поля/перевода; ru/en; изменение одного навыка не стирает остальные подписи. Без мировой БД. |
| `node --experimental-vm-modules --test header.test.mjs` | 8/8 | Настоящий _prepareCharacterData и модель: все шесть позиций, age0, Item/Actor приоритет, other с текстом/без, неизвестные коды, отсутствие записи прочерков. Sheet/base API и обогащение в стенде ограничены. |
| Существующие `form.test.mjs` и `progression.test.mjs` из `work-cat/witcher-task0010-012-20260918` | 15/15 | Источник базы отличается от prepared, ручные derived, цена/пределы повышения/нехватка ОУ, allow-list. Окна/запись БД подменены; не доказательство сохранения в мире. |
| `node --experimental-vm-modules layout.mjs` | 30 локальных отрисовок в Chromium; ошибок JS/загрузки ресурсов нет | Настоящие HBS/CSS/шрифты системы и core CSS. 18 вариантов редакторов ru/en, 1366×768 и900×700, масштабы1/1.25; 12 вариантов карточки ru/en (навыки/инвентарь/предыстория) при высоте730/1200. Нет горизонтального переполнения строк/полей редактора; раскрытие подробностей работает. Подписи внутри полосы, skill labels непустые, FormData пустых select остаётся пустым. |

Локальный layout-стенд собирает контейнер приложения и часть Handlebars helpers: это проверка DOM/CSS, а не жизненного цикла ActorSheetV2. Масштаб проверен увеличением корневого размера шрифта; настоящий ползунок масштаба Foundry ещё не проверен. Формы не отправлялись в игровую БД. Скриншоты `editor-*.png` / `character-*.png`, сводка `layout.json` (30 отрисовок и четыре дополнительные записи измерений полосы). Старый стенд .006 несовместим с текущими экспортами; регрессия выполнена актуальным стендом .012.

## Остаток игровой приёмки

Foundry при проверке доступности ответила **HTTP502**. Служба и права агентом не изменялись. После запуска проверить UI02–UI05/UI08–UI12 по плану: создание через каталог; реальное сохранение/перезагрузка; монстр; повышение из новых строк и отказ при нехватке ОУ; пустые select после изменения только имени; наполненный/перегруженный инвентарь; штатный масштаб Foundry и соседние окна. Проверить HTTP-доступ изменённых ресурсов. Не путать локальные проверки с игровыми; не закрывать .013 или issue-00015 до приёмки.

## Документация и справочник

Обновлены карточки фактически изменённых исходников, подписи/зависимости Skill, редактора, шапки и предыстории в графе; добавлены новые методы/контекст и ключи ru/en, перенесены прямые чтения шапки в подготовку контекста. Остальные слои графа не расширяются. Права, владельцы, группы и существующие inode сохраняются; новых игровых файлов нет.

Адресная сверка: синтаксис четырёх изменённых JS-файлов корректен; `git diff --check` без замечаний; 4388 локальных ссылок/якорей изменённых документов проверены. Подтверждено сохранение mode/uid/gid/inode всех2102 ранее отслеживаемых файлов; ru/en получили только12 новых ключей каждый. Три существующие проверки справочника PersistenceIndex (backend формы/кнопок, общий writer прокачки, отсутствие записи из prepare) прошли. CLI проверен для writer Skill.label и процесса подготовки шапки. Целостность/актуальность справочника: valid=true, current, differences=[];641 источник,5861 сущность,15888 связей,488 процессов. Полного прогона справочника не было.

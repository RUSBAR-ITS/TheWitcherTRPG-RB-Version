# TASK-0010 — Игровая проверка и регрессия 2026-09-19

**Принято и закрыто 2026-09-19, 14.3.1.00084:** пользователь подтвердил завершение [TASK-0010/.001–.019 и 27 полностью решённых issues](../tasks/task-0010-parameter-limits.md#closure). Частичная 00028 и отдельные TASK-0011/0012/0013 не закрыты. Ниже сохранён датированный протокол с фактическими версиями, охватом и ограничениями; прежние ожидания подтверждения завершены этим решением.

Проверены исходники **14.3.1.00082**, `dev`, HEAD `c9bdbf3a4f89b97bb3de3c49f2c8f74bf72c9473`. Foundry **14.367**, Chromium **153.0.8010.12**, мир **witcher-rb-test**, отдельные клиенты **QA-GM** и **QA-Player**. Пользователь разрешил игровые проверки и расширенную регрессию. Версия **14.3.1.00083** оформляет этот отчёт; игровой код, стили, локализации и компедиумы при проверке не исправлялись.

**Новых ошибок системы в проверенных сценариях не обнаружено. 403 синтетические проверки прошли.** Пройдены игровые сценарии расчётов/записи/окон/эффектов, повторная проверка интерфейса .014–.019 и регрессия контейнеров, таблиц, травм. Сняты прежние блокировки мастера AE и создания Item.skill. Это результат перечисленного охвата, не доказательство отсутствия ошибок во всех возможных сочетаниях системы.

[Задача и согласованные критерии](../tasks/task-0010-parameter-limits.md), [прежняя игровая матрица Б01–Б19](task-0010-013-browser-checks.md#browser-matrix), [статическая приёмка](task-0010-012-static-acceptance.md), [локальная проверка визуала .019](task-0010-019-visual-checks.md). Задача и полностью решённые issues закрыты по подтверждению пользователя в .00084. Частичный результат issue-00028 и отдельные TASK-0011/0012/0013 сохраняют свои границы.

## Стенд и доказательства

Артефакты: `/var/lib/foundryvtt/Data/work-cat/witcher-regression-20260919`. Основные: `report.json`, `static-final.txt`, `static-final-files.json`, `visual.json`, `filled-ui.json`, `http-verified.json`, `http-css.json`, `before-reload.json`, `after-reload.json`, `reload-diff.json`, `cleanup.json`; последовательность действий сохранена в отдельных `.mjs`.

- **72 JS/HBS/ru/en и все 36 CSS** загружены через HTTP200 и побайтово совпали с локальными файлами. Манифест клиента сообщает .00082.
- Игровые документы, БД, транспорт, методы системы, Roll и обработчики не подменялись. Часть входов вызвана через `page.evaluate`, после чего использовано настоящее окно; ниже это отделено от клика по исходной кнопке.
- Для проверки границ порога использованы постоянные формулы настоящего Roll. Для гарантированного попадания в отдельном сценарии onHit — `Roll('0')` против порога1 с данными настоящего Attack ChatMessage; это проверка обработки результата, а не вероятность попадания.
- Два диагностических снимка полностью собраны (`collected`, без пропусков сериализации) и сохранены сборщиком: `a2e6d070-7b97-400d-b157-f4d7502274ba` и `6084b361-c158-481d-b5b2-6446f26de1e5`. После reload ключ сборщика потребовалось заново передать тестовому клиенту; второй снимок доставлен из локального кэша, подтверждение в `snapshot-after-delivery.json`.
- Журнал событий использует ограниченную сериализацию и не является полным дампом каждого события. Доказательство сохранения — отдельное сравнение исходных JSON и расчётных значений до/после reload, а не отсутствие сообщений в журнале.

## Синтетическая регрессия

Команда: `python3 /var/lib/foundryvtt/Data/work-cat/witcher-regression-20260919/run-final-static.py`. Итог: **403 tests, 403 pass, 0 fail, 0 skipped**, 20 файлов тестов, около9секунд. Полный список входов — `static-final-files.json`.

Охвачены модель и валидация модификаторов, редактор/форма, вычисления и производные, сохранение базы/ресурсов, прокачка, сборщик бросков и потребители, семейства/назначение/часы/истечение, 125 проверок содержимого эффектов, жизненный цикл травм, контейнеры/извлечение, подписи навыков/шапка и удаление воздействия профессионального навыка.

Старые внешние стенды потребовали актуализации после рефакторинга: загрузчик не предоставлял новые импорты; имитация Collection не имела `get`; эффект не имел `toObject`; backend возвращал все документы вместо созданных. Проверка редактора ожидала прежнюю разметку. Исправлены только копии стендов в каталоге артефактов; исходный игровой код не менялся. Первые неуспешные прогоны сохранены отдельно, окончательный результат — `static-final.txt`.

Границы стендов: локальные тесты используют настоящий код вычислений и, где предусмотрено, модели/часы установленного ядра Foundry. Серверные записи и окна в них заменены адаптерами. В старых контейнерных тестах новые обёртки параметров/назначения изолированы нейтральными адаптерами; их интеграция отдельно проверена текущими тестами параметров и настоящими игровыми переносами. Эти локальные результаты не выдаются за проверку сервера.

## Игровые результаты

| Область | Проверено и результат | Вход / граница |
| --- | --- | --- |
| Б01, мастер AE | Выбор BODY, явные чекбоксы HP/STA/bodyDamage, сворачивание со сводкой, сохранение только STA, повторное открытие. Вторая строка STA×0.25 сохранилась. Очистка исключения меняет STA10→6, возврат —6→10. Переключение на «К броску» очищает/отключает исключения. Мастер не закрывает родительский редактор | Настоящие кнопки Item-конфигурации, редактора и мастера. Недопустимые два канала и nonStacking без ID отклонены моделью |
| Б02, численные каналы | 9+2 full→параметр10/вклад11; 3−5 full→1/−2, без full→1/1. Смешанные9+2+2→10/12. Только потолок допускает11, снятие обрезает10; строка без действий нейтральна | Реальные Actor/AE и общий контекст броска; не перебор всех возможных сочетаний |
| Б03/Б13, прокачка | Расовый навык6+2 стоит8 ОУ; штраф с влиянием на прокачку даёт цены6/7/8, снятие обрезает базу11 до10. Отрицательный навык: четыре шага по1; характеристика под минимумом: два шага по10. Магические3+обычные5 оплачивают стоимость8, две записи журнала; нехватка средств не меняет документ | Реальные методы покупки/записи; дополнительно кнопка повышения характеристики в редакторе |
| Б04/Б11, травмы | 18 стадий шести травм: сердце STA10/20/40, септический шок10/20/35, рёбра с адресными исключениями HP, две травмы ноги со штрафами SPD/навыков. Кнопки стабилизации/лечения сохраняют UUID, текущая STA10 не пополняется. Повтор возвращает начальную стадию без дубля. Заживление пересчитывается по действующему BODY и удаляет травму при отдыхе | Настоящий установленный pack; численная матрица импортирована через installWound, лечение/стабилизация нажаты на листе. Drag-and-drop всех94 травм не повторялся |
| Б04, другие производные | BODY8+2 с исключением bodyDamage сохраняет бонус урона2, без исключения даёт4. SPD8×0.5 и собственный Run+5 дают Бег17 | Настоящие AE/Actor; отдельный бросок ближнего урона для каждой комбинации не выполнялся |
| Б05, обычный бросок | Кнопка Убеждения: ручной+1 остаётся, выбираемый+1 входит только при выборе. Следующее окно снова без галочки; отмена без ChatMessage | Настоящая кнопка навыка и окна. Полный социальный поединок не проверялся |
| Б06, атака/защита | Стрельба: `1d10+8+6+2+1−1`; уклонение: `1d10+8+5+2+1`. Условный вклад однократный, чужой навык отсутствует среди кандидатов защиты | Вход через метод, далее настоящие окна. Близкая дистанция/торс/уклонение; все типы атак и дистанции не перебирались |
| Б07/Б16, профессия | Два одноимённых слота независимы; удаление через вложенную иконку меняет только выбранный слот. Прямая атака профессии и подстановка в оружейную дают `1d10+5+5+1−1`, где навык5=уровень3+свой модификатор2 | Настоящая конфигурация и окна атак; не весь набор профессиональных действий |
| Б07/Б17, Item.skill | Создание разрешено. Собственный навык с именем archery и после переименования даёт одинаковую формулу `1d10+8+6+1+3`; не подменяется встроенной Стрельбой. У монстра с dontAddAttr —`1d10+6+1+3` | Кнопка собственного навыка персонажа; метод собственного навыка монстра и настоящее окно. dontAddAttr — поле монстра, не персонажа |
| Б07/Б18, магия/ремонт | Отмена условного выбора заклинания не создаёт сообщения и не тратит STA; принятие даёт `1d10+8+4+2` и расход3. RepairSystem.prepareRollFormula с выбранным+1 даёт `1d10+8+1`, отмена возвращает null | Настоящие окна; полный ремонт/крафт/алхимия не объявлены проверенными |
| Б08, пороги | Настоящие Roll для−1/0/1/2, `<`/`<=` и отсутствующего порога. Бонус испытания меняет порог, не кубик. Кнопка испытания против смерти с порогом0 даёт автопровал | Проверка вероятности/рефакторинг критов вне этого прогона |
| Б09/Б14/Б15, база и ресурсы | HP35→max20 обрезает текущие до20; рост max40 не лечит. STA5 остаётся5. Удача7 при снижении max8→3 обрезается до3, возврат max8 не пополняет ресурс. При AE+2 соседнее изменение потолка не записывает действующее значение вместо базы8. Кнопка повышения: база9/итог11, списано80 ОУ. Авто-HP только для чтения; ручной HP монстра31 сохраняется, покупки монстру недоступны | Реальные формы и запись; значения сверены после reload |
| Б10/Б12/Б19, семейства/доставка | Повтор на Actor заменяет прежнюю запись; у двух вещей действует новая, снятие/удаление/истечение возвращает доступную старую. QA-Player без права записи передаёт GM воздействие+2 и срок3; активный GM сохраняет его. Нулевой срок допустим. Улучшение оружия damage2d6 со сроком4, повтор со сроком3 заменяет запись, отмена не меняет оружие | Два клиента, настоящие записи и выбор оружия; ошибки сети/отсутствие владельца не инъектировались |
| Б10/Б19, боевые часы | Назначение до хода цели корректирует срок1→0 ровно один раз, сохраняет нужного combatant. На ходе цели временный победитель истекает и возвращается+1 второго источника. Одновременное истечение+10/−10 к HP/STA оставляет40/40; повторено и по мировым секундам | Настоящий Combat с тремя QA-акторами и мировые часы. Часы возвращены0, Combat удалён |
| Б11, периодика | Штатное кровотечение2 плюс последствие сердца+2 снимают4 HP при ходе:40→36. Изменение флага Combat не повторяет урон | Настоящий updateCombat на активном GM; чужие акторы не участвовали |
| Б19, Attack→Damage→AE | Настоящее заклинание формирует Attack со сроком3; onHit передаёт+1 REF. Его Damage ChatMessage сохраняет срок3; применение урона через окно снимает2 HP и передаёт+1 DEX. Исходные эффекты сохраняют срок9 и флаги применения; копии имеют срок3 и снятые флаги | Вход поражения через handleDefenseResults с постоянным Roll; урон через ApplyNormalDamage и настоящее окно. Источники на принадлежащем Actor Item имеют штатный start; это не отдельные world-шаблоны |

## Интерфейс .014–.019 и прежние исправления

- **14 нативных переключений**: все7 вкладок в ru/en сохраняют высоту800. Названия встроенных навыков нового Actor видны до редактирования. Пустая шапка содержит6 прочерков, без ключей WITCHER; заполненная правильно берёт расу, профессию, пол, возраст32, родину из Item и локализованное общественное положение.
- **12 случаев заполненного листа**: ru/en × размеры viewport1366×900/1920×1080 × параметры/инвентарь/магия; высоты окна800/950 сохраняются, горизонтального переполнения выбранных вкладок нет. Дополнительно исправлен тип тестовых заклинаний на действующий `Spells` и проверены3 видимые строки магии в каждом языке. Плюсы секций выровнены по правому краю.
- В обоих настоящих редакторах колонка имени равна самому длинному тексту, отступ до чисел10–20px; полные имена и кнопка «Повысить параметр». Шкала шрифтов и подписи видны на сохранённых снимках. Субъективное принятие внешнего вида остаётся за пользователем.
- Мастер AE проверен на1366×768 и1920×1080: нет горизонтального переполнения; до кнопки можно прокрутить. Применение/отмена оставляют основной редактор.
- После3 частичных рендеров один клик конвертера открывает один DialogV2; кнопка мастера AE не дублируется. Полные операции обмена валюты не повторялись.
- Контейнеры: независимая копия из мира; копия наполненного шаблона с новыми UUID всего дерева; перемещение/извлечение на одном Actor без замены UUID; полный перенос между Actor; копирование вещи Actor в world-шаблон сохраняет источник; отказ от удаления сохраняет дерево, подтверждение удаляет контейнер с содержимым. Использованы настоящие методы размещения и окно удаления; все drag-and-drop маршруты не перебирались.
- Прочитаны **все7 установленных компедиумов**, **128 RollTable и94 Item**; criticalWounds содержит4 папки и77 AE.62 найденные в содержимом ссылки `@UUID` разрешились. Это чтение действующих документов и выбранных ссылок, не повторная сверка всего контента с книгой. Packs не собирались, не изменялись и не открывались через внешний LevelDB.
- Временная цепочка из11 настоящих RollTable успешно возвращает лист на глубине10. Ещё один уровень отвергается установленным ограничением. Таблицы после проверки удалены.

## Сохранение, ошибки сценариев и очистка

После reload **обоих клиентов** совпали численные результаты, документы эффектов/предметов и таблиц: **15 Actors, 8 world Items, 12 RollTables**. При сравнении исключены серверные `_stats`. Остальные1522 отличия исходных JSON —761 на клиент — только заполнение пустых `system.skills.*.*.label`; каждое сверено с точной подписью CONFIG.WITCHER.skillMap. Ни одно различие чисел, условий, ссылок, сроков или состава предметов не обнаружено.

`report.json` сохраняет и ошибки самих сценариев: устаревшие селекторы/имена вкладок и полей, закрытые details после рендера, вызов метода массива у Collection, предположение о пустом start у эффекта принадлежащего Actor предмета, неверное ожидание базового кровотечения5 вместо настроенного2. Они устранены в тестах и проверены повторно; не скрыты как удачные первые запуски. Предположение о dontAddAttr у персонажа исправлено отдельным тестом монстра. Неожиданных `pageerror` нет. Шесть сообщений console.error относятся к двум намеренно недопустимым моделям и сообщению отказа записи; это ожидаемые проверки валидации.

Удалены15 QA-акторов,8 world Items,12 таблиц,20 сообщений и временный Combat. Сравнение исходных и конечных ID Actors/Items/Tables/Messages/Scenes/Folders/Combats: **нет остатка и нет пропавших исходных документов**. Контрольные два персонажа пользователя не менялись во время боевого теста. Время0, языкru и исходно выключенный журнал тестового клиента восстановлены; браузеры закрыты. Правами, службой и настройкой мира агент не занимался.

## Границы результата

- Проверен согласованный рефакторинг и репрезентативная регрессия зависимых механизмов. Все профессии, все сочетания экипировки/статусов, все сценарии социального боя/крафта/ремонта и внешние модули не перебирались.
- Не имитировались обрывы сети, недоступный адресат, конкурентные операции и посторонние изменения во время записи. Ранее принятые ограничения остаются в [known-limitations.md](../documentation/known-limitations.md).
- Непроверенные здесь самостоятельные ошибки из TASK-0011/0012/0013 не становятся исправленными. Частичный объём00028 сохраняется.
- Технический прогон .013/.014–.019 выполнен в указанном охвате; закрытие задач и полностью решённых issues, включая принятие внешнего вида, подтверждено пользователем в .00084.

## Проверка оформления

Обновлены отчёт, навигация/состояние приёмки, CHANGELOG и счётчик версии. В справочнике меняются только хеши манифеста/его карточки и контролируемых документов; структура/семантика графа прежняя. Проверены1126 локальных ссылок/якорей изменённых Markdown — без ошибок; `git diff --check` без замечаний. Сохранены inode/права/владельцы/группы всех существующих отслеживаемых файлов. Единственная правка вне docs — счётчик system.json. Выполнен один `python3 -B docs/analytics/system-index/query.py check --freshness --format json`: valid=true, freshness=current, differences=[]. Полный набор тестов механизма справочника не запускался: механизм не менялся.

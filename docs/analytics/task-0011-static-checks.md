# TASK-0011 — Статические проверки исправлений

<a id="task-0011001"></a>
## TASK-0011.001 — восстановление Выносливости и индикатор HP

**2026-09-19, 14.3.1.00101.** Ветка `dev`, база `7557e294d27102e59338e79a7796b81a299f6077` плюс описанные правки. [Подзадача](../tasks/task-0011-modifier-integration-followups.md#task-0011001) реализована и проверена локально; игровая приёмка B01 ещё не выполнена. Остальные подзадачи .002–.010 этим отчётом не объявляются выполненными.

### Реализация

- В [WitcherActorSheet](../../module/actor/sheets/WitcherActorSheet.js) и [WitcherActorSheetV1](../../module/actor/sheets/WitcherActorSheetV1.js) Recovery Action пишет `min(sta.max, sta.value + rec.value)`. Значения берутся при подтверждении; guard полного запаса, уведомление, Full Recovery и ожидание Actor.update сохранены. Регистрация V1 не менялась.
- В [sidebar персонажа](../../templates/sheets/actor/partials/character/sidebar.hbs) и [sidebar монстра](../../templates/sheets/actor/partials/monster/sidebar.hbs) целое сердце определяется по действующему `hp.max`. Операция сравнения `>=`, порог ранения и цвета сохранены. Максимумы, текущие ресурсы и temporaryHpSum шаблонами не меняются.
- Новых интерфейсных подписей нет; существующая локализация сохранена. Формулы производных параметров, схема ресурса, компедиумы и права файлов не менялись.

### Поведенческие проверки

**36/36 прошли**, пропусков нет. На исходном коде .00100 эти же сценарии дали **26 успешных и10 неуспешных**: шесть проверок переполнения REC и четыре проверки сердечка при изменённом максимуме. После исправления все36 проходят. Синтаксис обоих JS проверен `node --check`.

| Область | Проверено |
| --- | --- |
| Recovery V2/V1 | 9/10+3→10; 4/10+3→7; 0/10+3→3; 2/5+8→5 |
| Полный/превышенный запас | Для обеих кнопок при10/10 и12/10 нет записи, есть прежнее уведомление; текущий избыточный запас не срезается кнопкой восстановления |
| Данные на момент подтверждения | Изменённые после открытия value/max учитываются; Full Recovery использует актуальный max |
| Закрытие/ожидание | Открытие без выбора не пишет данные; оба callback ожидают Promise Actor.update и передают отказ записи наверх |
| Обе панели HP | 40/60 при исходном40 — разбитое зелёное; 35/35 при исходном40 — целое зелёное; 5/40 при threshold10 — оранжевое; ровно threshold — разбитое зелёное; 45/40 — целое |
| Отображение и данные | Весь HBS рендерится, число max правильное, входные данные не мутируют, временные ПЗ не подменяют заполненность постоянного запаса |

**Методика и пределы:** импортированы настоящие ES-модули обоих листов через vm.SourceTextModule; исполняются их реальные `_onRecoverSta` и callbacks. Базовые классы Foundry, неиспользуемые примеси, DialogV2, локализация/уведомления и Actor.update подменены. Полные реальные HBS обработаны установленным Handlebars с фасадами стандартных helpers. Запись БД, сеть, native окно и реальная регистрация обработчиков этим не подтверждены.

Артефакты: `/var/lib/foundryvtt/Data/work-cat/witcher-task0011-001-20260919/`: `resources.test.mjs`, `before-tests.log`, `after-tests.log`, baseline с хешами/метаданными. Запуск:

```bash
node --experimental-vm-modules --test /var/lib/foundryvtt/Data/work-cat/witcher-task0011-001-20260919/resources.test.mjs
```

### Адресная сверка справочника

Обновлены две сущности wound-state, их чтение `stat().max` вместо `stat().unmodifiedMax`, REC callbacks и процессы ожидания записи. Уточнены сохранившиеся исторические формулировки «без ожидания» в Full Recovery: await уже был в коде до этой порции. После добавления двух строк в каждый JS перенесены адреса зависимых записей; новые сущности и процессы не добавлены, IDs сохранены.

Прошли **3 адресных теста** `ResourceFormExpansion` из `test_expansion_024.py`: `test_source_prepared_maximum_and_display_remain_distinct`, `test_recovery_guards_cap_and_awaited_update`, `test_sidebar_control_contracts_and_reverse_writers`. Первый теперь проверяет текущий контракт sidebar и его чтения; устаревшие строковые ожидания старого вычислителя из TASK-0010 не используются как проверка этой UI-правки. Поведенческая проверка формулы — отдельные36 сценариев выше, не поиск строки Math.min.

Команда из `docs/analytics/system-index/tests`:

```bash
python3 -B -m unittest test_expansion_024.ResourceFormExpansion.test_source_prepared_maximum_and_display_remain_distinct test_expansion_024.ResourceFormExpansion.test_recovery_guards_cap_and_awaited_update test_expansion_024.ResourceFormExpansion.test_sidebar_control_contracts_and_reverse_writers -v
```

Полный набор справочника не запускался. Итоговая проверка ссылок/метаданных и один `query.py check --freshness --format json` выполняются после обновления документов и хешей; протокол — `final-check.json` в каталоге артефактов.

### Что осталось

Игровая B01 по команде пользователя: штатная кнопка восстановления, эффект изменения max и reload; сердечко персонажа и монстра. Issues00164 и00203 сохраняют статус potential до дальнейшего решения пользователя; они не закрываются по синтетическим результатам. Следующая порция реализации — TASK-0011.002.


<a id="task-0011002"></a>
## TASK-0011.002 — локальная обёртка навыков

**2026-09-19, 14.3.1.00102.** Ветка `dev`, база `3f0538de820dcd83265ef38d00adaa3002d1d650` плюс эта правка. [Подзадача](../tasks/task-0011-modifier-integration-followups.md#task-0011002) реализована локально; B02 ещё впереди. Запись .001 выше описывает предыдущую порцию.

В [skillMixin](../../module/actor/sheets/mixins/skillMixin.js) глобальное присваивание заменено на `const $html = $(html)`, два `.find().on()` используют эту переменную. Четыре маршрута — профессия, сворачивание группы, бросок навыка и повышение навыка — сохранены вместе с actor/skillMap. Расчёты, разметка, локализация и компедиумы не менялись.

**S02: 6/6 прошли** (до исправления те же шесть не проходили):

- Общие и разные функции `$`/`jQuery`, отсутствующее имя `jQuery`, доступная только для чтения глобальная `jQuery`: четыре клика работают, глобальные функции/дескрипторы неизменны.
- Четыре вызова регистрации с новой разметкой: по одному действию каждого вида на клик.
- Два листа: каждый передаёт событие, запись панели, запись skillMap и ключ повышения своему Actor.

Настоящий ES-модуль импортирован через `vm.SourceTextModule`, вызваны реальные skillListener и _onSkillDisplay. Использован Node EventTarget для отправки кликов; DOM/querySelectorAll/closest, jQuery-обёртка и Actor — минимальные фасады. Библиотека jQuery, полный render Foundry, покупка уровня/бросок и запись БД не исполнялись. Повторная регистрация на одном и том же DOM не заявляется исправленной; браузерный цикл проверяется отдельно в B02. Синтаксис прошёл `node --check`.

Артефакты: `/var/lib/foundryvtt/Data/work-cat/witcher-task0011-002-20260919/`, файлы `skill-listener.test.mjs`, `before-tests.log`, `after-tests.log`, `baseline.json`. Команда:

```bash
node --experimental-vm-modules --test /var/lib/foundryvtt/Data/work-cat/witcher-task0011-002-20260919/skill-listener.test.mjs
```

### Справочник и документация

Обновлены описание skillListener, внешняя граница вызова `$(html)` и связь к ней (`calls` вместо записи в глобальную переменную); ID сохранены. В процессах регистрации V2 и редактора убраны утверждения о глобальном присваивании. Обновлены карточка исходника, issue-00167, задача, навигация и версия. Исторические опыты остаются датированными свидетельствами прежнего кода.

Адресный тест `SkillUIExpansion.test_render_listener_dispatch_and_old_dom_contract` прошёл (1/1): проверяет эти связи и существующие маршруты слушателей. Устаревшие жёсткие адреса родительских листов и ожидание старого селектора customSkillListener актуализированы по текущему коду; сам customSkillListener не менялся. Полный набор справочника не запускается. Команда из `docs/analytics/system-index/tests`:

```bash
python3 -B -m unittest test_expansion_009.SkillUIExpansion.test_render_listener_dispatch_and_old_dom_contract -v
```

Итог ссылки/метаданные и один freshness-check — в `final-check.json` каталога артефактов. Игровая B02 ждёт команды пользователя; issue-00167 остаётся potential до решения о статусе. Следующая реализация — .003, UI словесного боя.


<a id="task-0011003"></a>
## TASK-0011.003 — меню и независимые формы словесного боя

**2026-09-19, 14.3.1.00103.** Ветка `dev`, HEAD `3f0538de820dcd83265ef38d00adaa3002d1d650` плюс сохранённые незакоммиченные изменения .002 и текущая .003. [Подзадача](../tasks/task-0011-modifier-integration-followups.md#task-0011003) реализована локально; B03 не запускалась.

### Реализация и зависимости

- [verbalCombat.js](../../module/scripts/verbalCombat/verbalCombat.js): условие меню — Boolean найденного маркера, total — textContent найденного DOM-элемента. Пустой/non-finite результат даёт уведомление до выбора Actor; `0` допустим. Actor выбирается прежним getInteractActor с его уведомлением при отсутствии. Callback ожидает applyVerbalCombatDamage, тот — Actor.update. Формула `resolve.value - floor(totalDamage)` сохранена, новый минимум/максимум не вводится.
- [verbalCombatDefense.js](../../module/scripts/verbalCombat/verbalCombatDefense.js): такой же контракт меню, ожидание открытия защиты. В legacy callback используется только переданный корень (HTMLElement либо html[0] jQuery), проверяются группа Defenses и собственный ключ действия в CONFIG. Radio и customModifiers берутся из этого корня. Counterargue возвращает Promise штатного actor.verbalCombat; обычная защита сохраняет prepareCheck, `>=`, threshold, RollConfig и flags.
- [Actor mixin](../../module/actor/mixins/verbalCombatMixin.js): radio читается из button.form, отсутствие выбора даёт уведомление/null. Перед доступом к действию проверяются собственные ключи group/action в CONFIG. Формирование урона, фактический skill, action verbalAttack и передача manual в prepareCheck сохранены.
- [Атака](../../templates/dialog/verbal-combat.hbs) и [защита](../../templates/dialog/verbal-combat-defense.hbs): каждый radio вложен в свою подпись label, глобальные id/for удалены. Имена групп формы и набор действий сохранены; защита передаёт явный data-group=Defenses. Прежний выбор по умолчанию не перенастраивался.
- [ru](../../lang/ru.json)/[en](../../lang/en.json): только добавлены `WITCHER.verbalCombat.InvalidTotal` и `InvalidAction`. Прежние ключи и тексты не менялись; выбор Actor использует существующий `WITCHER.Context.SelectActor`.

Неиспользуемый массовый addVerbalCombatChatListeners не подключался и не исправлялся. Контекстные меню других процессов, сравнительная механика, расчёт модификаторов, содержимое компедиумов и статус issues прежние.

### S03 — результаты

**38/38 прошли**, пропусков нет; синтаксис трёх JS прошёл `node --check`. Те же сценарии на сохранённых исходниках до .003: 37 неуспешных, одна проверка текущих текстов ru/en успешна (эта проверка намеренно использует актуальные локализации).

| Проверка | Результат |
| --- | --- |
| Два меню, маркер есть/нет | Строгие true/false для native DOM-фасада |
| Нет total, пусто, пробелы, NaN, Infinity, 12abc | Уведомление; нет выбора действия, записи или броска |
| Нет Actor | Реальный getInteractActor сообщает существующим ключом; меню останавливается |
| Resolve total 0, 3.9, −1.5 | Из 10 получено соответственно 10, 7, 12 — прежняя арифметика floor без нового clamp |
| Ожидание записи | До разрешения update Promise callback не завершён, отказ записи передаётся вызывающему коду |
| Правильный выбор атаки/защиты | Фактические builtin skill, action, manual, dmgStat, тип ChatMessageData, RollConfig и flags сохранены; threshold0 допустим |
| Нет radio, чужая группа, неизвестное имя, inherited constructor | Предупреждение и остановка до prepareCheck/броска/записи |
| Отмена prepareCheck | Нет extendedRoll и записи ресурса |
| Два окна каждого вида | Отдельные radio/manual; у защиты также свои thresholds; проверены native/jQuery корни |
| Counterargue | Одно делегирование штатному действию Actor, возвращаемый Promise ожидается |
| Два HBS и ru/en | Реальный Handlebars создаёт все действия, label содержит свой input, нет общих id/for; новые ключи есть в двух языках |

**Методика:** импортированы полные настоящие ES-модули трёх обработчиков, CONFIG, helper.getInteractActor, ChatMessageData и RollConfig через vm.SourceTextModule; реальные HBS с Handlebars. Контекстное меню/окна/DOM/Actor.update, prepareCheck и extendedRoll заменены фасадами/регистраторами. Проверяется фактический контракт передачи в общий механизм, а не новый прогон расчёта модификаторов или броска кубов. Глобальный document.querySelector в стенде бросает ошибку: обращение к чужой форме не может случайно пройти тест. Два окна представлены независимыми callback/корнями, а не открытым браузером. Клик по label, оформление, запись БД и реальный render ещё требуют B03.

Сверен установленный `/opt/foundryvtt/client/appv1/api/dialog-v1.mjs`, submit:215–225: callback получает текущий корень согласно options.jQuery; старый Dialog сам не ожидает Promise callback и закрывается сразу. Изменение сохраняет этот API, но Promise асинхронных операций не теряется внутри наших обработчиков. Это не обещание держать legacy-окно открытым до броска.

Артефакты: `/var/lib/foundryvtt/Data/work-cat/witcher-task0011-003-20260919/`: `verbal-combat.test.mjs`, `before-tests.log`, `after-tests.log`, `baseline.json`, исходники до правки и `graph-before.json`. Запуск:

```bash
node --experimental-vm-modules --test /var/lib/foundryvtt/Data/work-cat/witcher-task0011-003-20260919/verbal-combat.test.mjs
```

### Справочник и приёмка

Сверены точки регистрации обоих меню, общая цепочка prepareCheck/extendedRoll и два процесса словесного действия. Описаны локальные guard/return и Counterargue; устранена пометка о ещё неисправленной области выбора legacy. Адреса изменённых исходников обновлены. Большинство дополнительных строк diff в графе — сдвиг адресов существующих записей ru/en после добавления двух ключей, без изменения их семантики. Новые сущности/отношения/процессы не добавлялись, ID и partial-охват сохранены.

**1/1** адресная проверка `ConsumerIndex.test_consumers_reach_shared_check` прошла. Из `docs/analytics/system-index/tests`:

```bash
python3 -B -m unittest test_task0010_consumers.ConsumerIndex.test_consumers_reach_shared_check -v
```

Полный набор справочника и ранее успешные несвязанные тесты .001/.002 повторно не запускались. Итоговые ссылки, метаданные доступа, сохранность предыдущей .002 и один check --freshness отражаются в `final-check.json`. Issues00302/00303 остаются potential до подтверждения закрытия. Следующая порция реализации — .004 (алхимия и ремонт); игровые B01–B03 ждут команды пользователя.


<a id="task-0011004"></a>
## TASK-0011.004 — алхимия и ремонт

**2026-09-19, 14.3.1.00104.** Ветка `dev`, предыдущие незакоммиченные .002/.003 сохранены. [Согласованный план](../tasks/task-0011-modifier-integration-followups.md#task-0011004) реализован локально. Foundry, браузер и мир не запускались; B04 впереди.

### Что изменено

- [WitcherCharacterSheet](../../module/actor/sheets/WitcherCharacterSheet.js): `_alchemyCraft` читает `item.alchemyCraftComponentsList`. Существующий getter, девять субстанций, рецепт +2, выбор alchemy/crafting и `realCraft` сохранены.
- [WeaponData](../../module/data/item/weaponData.js) и [ArmorData](../../module/data/item/armorData.js): getter `damagedLocations` возвращает массив повреждённых значений с локализованными `label`, `reliabilityValue`, `maxReliabilityValue`. Оружие — надёжность; броня — надёжность и шесть зон SP. Пределы совпадают с существующими canBeRepaired/repair, изменённый эффектами SP не подставляется вместо базового maxStoppingPower. `repair()` возвращает Promise parent.update.
- [RepairSystem](../../module/item/systems/repair.js): RepairData делегирует список модели. repairItem проверяет техническую возможность до реального действия, _doRepair повторяет проверку перед расходом. Проверяются повреждение, missing/unknown, актуальное количество у executor (включая несколько записей одного ID), местное право обновления либо activeGM. По одной единице каждой записи компонентов списывается последовательно с await; обычный неуспешный бросок по-прежнему расходует материалы.
- Восстановление через `_restoreItem`: при местном праве ждёт `restoreReliability → item.system.repair`; иначе ждёт зарегистрированный `activeGM.query` с `function: restoreReliability`, UUID Item и `data: []`. [Item repairMixin](../../module/item/mixins/repairMixin.js) возвращает этот Promise дальше в существующий [query](../../module/setup/queries.js). Общий dispatcher не менялся. Удалены emitForGM и вызов отсутствующего getRestoreReliabilityData из этой цепочки.
- Нет GM до действия — остановка без броска/расхода. После отправки отсутствие подтверждения true даёт предупреждение: материалы уже могли быть потрачены, предмет нужно проверить. Автоматической повторной отправки, возврата материалов или утверждения о неприменённом ремонте нет. При отказе местной записи исключение передаётся вызывающему коду; транзакционного отката нет.
- commonRepair ждёт _doRepair и публикацию результата. GM Repair сохраняет прямое восстановление без обычного броска/материалов: HTML с прежними повреждениями готовится до восстановления, сообщение создаётся после него. `sendRepairInfoToChat(data,isRequest,content=null)` допускает этот подготовленный HTML и ожидает ChatMessage.create. Simulation только бросает/публикует.
- В [en](../../lang/en.json) и [ru](../../lang/ru.json) добавлены только `WITCHER.Repair.alerts.noActiveGM` и `unconfirmedRepair`; прежние ключи не менялись. Существующие HBS ремонта совместимы со списком и оставлены без изменений. Диаграммы, цены, DC, строгое `total > threshold`, компедиумы и общая механика модификаторов не менялись.

### S04 — результаты и границы

**38/38 сценариев прошли, без пропусков.** На сохранённых исходниках до .004 — 34 неуспешных и 4 успешных. Синтаксис пяти изменённых JS проверен `node --check`.

| Сценарий | Подтверждено локально |
| --- | --- |
| Алхимия: simulation, обычный успех, crafting вместо alchemy | Настоящий getter и прежний realCraft достижимы; рецепт +2, нужный skill, создание результата и расход субстанций |
| Алхимия: нехватка, отмена prepareCheck, неуспешный бросок | Нет создания/расхода |
| Список повреждений | Только повреждённые значения оружия/брони, локализованные подписи, пустой массив; реальные HBS формы и чата рендерятся |
| Полный и каждый отдельный ремонт | Надёжность оружия/брони и все шесть SP восстанавливаются до базовых максимумов |
| Уже целое, нет рецепта, материала, quantity0, удалённый компонент | Нет обычного броска, расхода и восстановления |
| Повторяющийся ID в рецепте | Требуемое количество суммируется по одной единице записи; недостаток останавливает до расхода |
| Отмена prepareCheck | Нет броска/расхода |
| Успех/неуспех | Строгое сравнение сохранено; total=threshold и ниже расходуют материалы без восстановления |
| Simulation и GM Repair | Simulation не пишет предмет/материалы; GM не бросает и не расходует, чат сохраняет список прежних повреждений |
| Ремесленник и владелец | Проверка и материалы у executor, ремонтируемый Item у owner; использован настоящий обработчик query |
| Ожидание записи | Цепочка model → RepairSystem → Item → query не завершается до контролируемого update; ошибка записи передаётся вверх |
| Нет GM заранее | Остановка до броска/расхода |
| Timeout, false, запись с потерянным ответом | Предупреждение, один запрос/расход, без автоматического повтора и итогового сообщения об успехе; при выполненной записи предмет восстановлен |
| Изменение количества во время броска | Повторная проверка до списания останавливает ремонт |

**Методика:** реальные полные ES-модули листа, WitcherItem, моделей, RepairSystem, repairMixin, costEditMixin, CONFIG, query, ChatMessageData и RollConfig импортированы через vm.SourceTextModule. Неиспользуемые зависимости, API Foundry, Actor/Item update/remove/add, Dialog и транспорт query заменены фасадами. prepareCheck/extendedRoll проверяются как контракты передачи данных, расчёт модификаторов и кубов повторно не тестировался. HBS обработаны настоящим Handlebars с доступом к prototype getters. Запись мира, сетевые права, реальные окна/кнопки и transport timeout остаются для B04.

`realCraft` оставлен прежним, включая его собственные не полностью ожидаемые операции. Алхимические проверки на фасадах подтверждают данные/вызовы потребителя, не атомарность создания и списания на сервере. Ремонт также не является транзакцией; уже сделанное списание при произвольном сбое не откатывается.

Артефакты: `/var/lib/foundryvtt/Data/work-cat/witcher-task0011-004-20260919/`: `repair-alchemy.test.mjs`, `before-tests.log`, `after-tests.log`, `baseline.json`, копии исходников, `graph-before.json`, `graph-snapshot/`, `final-check.json`. Запуск:

```bash
node --experimental-vm-modules --test /var/lib/foundryvtt/Data/work-cat/witcher-task0011-004-20260919/repair-alchemy.test.mjs
```

### Справочник и остаток

Карточки семи изменённых исходников и манифеста обновлены. В граф добавлены 10 адресных сущностей (в том числе существующий getter алхимии) и 26 связей; три устаревшие связи ремонта с emitForGM/socketMessage удалены с резервированием ID. Уточнены описания Promise, процесс commonRepair и область алхимии. Partial-охват сохранён, адреса изменённых исходников обновлены; сдвиги ru/en механические.

Адресно проверяются **3 теста** из `docs/analytics/system-index/tests`:

```bash
python3 -B -m unittest \
  test_expansion_020.ChatDeliveryExpansion.test_repair_input_and_delivery_use_distinct_identifiers \
  test_task0010_consumers.ConsumerIndex.test_consumers_reach_shared_check \
  test_task0010_consumers.ConsumerIndex.test_explicit_replacement_and_repair_calls_are_present -v
```

Один `query.py check --freshness --format json`, ссылки изменённых документов, сохранность предыдущих изменений и метаданные фиксируются в `final-check.json`. Полный набор справочника и ранее успешные несвязанные тесты не повторяются. Issues00037/00102 остаются potential до подтверждения закрытия; следующая порция — .005, игровые B01–B04 ждут команды пользователя.


<a id="task-0011005"></a>
## TASK-0011.005 — хранение экипировки и суммарный EV

**2026-09-19, 14.3.1.00105**, ветка dev. Предыдущие незакоммиченные .002–.004 сохранены. [План](../tasks/task-0011-modifier-integration-followups.md#task-0011005) реализован; Foundry/браузер/мир не запускались, B05 впереди.

### Изменения

- [containerOperations](../../module/item/containerOperations.js): locationPatches объединяет isStored и снятие equipped в одном патче Item; существующие previousPatches/restorePatches сохраняют оба поля при отказе перемещения. При извлечении хранимый предмет остаётся снятым, включая старое противоречивое isStored/equipped. Поле equipped не создаётся у типов, где его нет.
- createCopy получает `stored` от назначения и выставляет isStored/equipped до createDocuments: скопированная надетая вещь не успевает включить переносимый бонус. place сохраняет false при drag с sourceContainer, включая перенос на другого Actor-монстра; обычный импорт оружия монстру сохраняет прежнее автоэкипирование. UUID/права/снимки/порядок создания и удаления источника прежние.
- [containerTemplates](../../module/item/containerTemplates.js): expandTemplates снимает equipped у всех вложенных узлов до записи единой пачки, если это поле присутствует. Новые UUID, переназначение внутренних origin и исходный шаблон сохраняются по прежнему контракту. Логика веса/вместимости не менялась.
- [armorMixin](../../module/actor/mixins/armorMixin.js): getArmorEcumbrance берёт только equipped armor с !isStored, затем один раз вычитает общую ignoredArmorEncumbrance и ограничивает нулём. Слои/SP/естественная броня не менялись; getLocationArmor по-прежнему использует getList.
- [castSpellMixin](../../module/actor/mixins/castSpellMixin.js): compensation=min(EV,max(0,ignoredEvWhenCasting)); в формулу входят −EV и только эта компенсация. EV уже получен после общего игнорирования. Подписи двух частей сохранены через прежние ключи локализации; manual/optional, prepareCheck, затраты и доставка заклинания не менялись.

WitcherItem.updateDocuments, isSourceSuppressed, семейства и withParameterChanges не переписывались: сброс equipped проходит через существующее обновление Item, выключает источник и завершает нормализацию ресурсов. Массовой миграции, изменения компедиумов, часов эффектов, локализаций и UI нет.

### S05 — 72 локальных сценария

| Набор | Результат |
| --- | --- |
| Новые сценарии контейнеров и EV | 18/18: weapon/armor у character/monster, store/move/extract/manual equip, копии world/pack, межвладельческий перенос, вложенные шаблоны world/character/monster, состояние до create, отсутствие лишнего equipped, старые противоречивые записи, восстановление после отказа записи |
| Колдовство | 28/28: EV0/1/3 × компенсация0/1/3/5 × подробности on/off, сумма двух EV1 с компенсацией3, общая компенсация один раз, отрицательная компенсация не усиливает штраф. Manual+7 и условный вклад+4 не обрезаются, стоимость STA сохранена |
| Переносимые AE и ресурс | 2/2: реальный Item.updateDocuments → isSourceSuppressed/семейство/withParameterChanges. Снятие более нового бонуса HP+20 возвращает доступный HP+10, max60→50 и текущие60→50. Извлечение не меняет результат; ручное экипирование повышает max до60 без лечения. Единственный бонус снимается с max60→40 и текущими55→40 |
| Прежняя контейнерная регрессия | 24/24: дерево/вес, самостоятельные UUID, source-preservation, origin, права, циклы, отказ/частичная запись и прежняя компенсация, удаление/импорт/дублирование |

На сохранённых исходниках до .005 те же **48 новых сценариев**: 25 неуспешных, 23 успешных. Первоначальный запуск текущего стенда выявил отсутствие addItem у тестового Actor; фасад исправлен, повторены 18 соответствующих сценариев. Ошибки рабочего кода на этом шаге не скрывались. Синтаксис четырёх изменённых JS прошёл node --check.

**Методика и пределы:** полные реальные ES-модули containerOperations/containerTemplates/WitcherItem, sheet itemMixin и armorMixin; документы/коллекции, permissions и I/O — фасады из прежнего контейнерного стенда. В двух отдельных сценариях подключены настоящие модульные расчёты/семейства/parameterPersistence и установленная клиентская подготовка Foundry из стенда TASK-0010.009; запись документов остаётся в памяти. Полный castSpell выполняется с фасадами окна/ChatMessage/записи, prepareCheck отдаёт фиксированную проверяемую основу, extendedRoll записывает формулу. Сетевая запись, реальные перетаскивания/интерфейс, reload и несколько клиентов проверяются в B05; локальные результаты их не заменяют.

Артефакты: `/var/lib/foundryvtt/Data/work-cat/witcher-task0011-005-20260919/`: `harness.mjs`, `equipment.test.mjs`, `spell.test.mjs`, `effects.test.mjs`, `container-regression.test.mjs`, `after-tests.log`, `equipment-tests.log`, `effects-tests.log`, `before-tests.log`, `baseline.json`, копии исходников/графа и `final-check.json`. Воспроизводимый запуск полного адресного набора:

```bash
node --experimental-vm-modules --test /var/lib/foundryvtt/Data/work-cat/witcher-task0011-005-20260919/{equipment,spell,effects,container-regression}.test.mjs
```

### Документация и граф

Обновлены четыре карточки файлов, версия/манифест, issues00254/00277, задача, навигация и журнал. В графе уточнены существующие операции хранения/копирования/извлечения, расчёт EV и его компенсация; добавлены шесть связей записи equipped. Связь EV→isStored теперь reads вместо прежней пометки о пропуске поля; ID сохранён. Сдвиги строк механические, partial-охват сохранён.

Адресные проверки: отбор брони/слои, компенсация магии/prepareCheck, drop/извлечение контейнера и общая связь consumers→prepareCheck. Полный набор справочника не запускался; один check --freshness, ссылки и метаданные отражены в final-check.json. Issues остаются potential до подтверждения закрытия. Следующая порция — .006, B01–B05 ожидают команды пользователя.

<a id="task-0011006"></a>
## TASK-0011.006 — источник, исполнитель и достоверный результат

**2026-09-19, 14.3.1.00106**, dev, база `3f0538de820dcd83265ef38d00adaa3002d1d650` и сохранённые незакоммиченные .002–.005. [Подзадача](../tasks/task-0011-modifier-integration-followups.md#task-0011006) реализована локально. B06 ещё не проводилась.

### Реализация и границы

- [effectDelivery.js](../../module/scripts/effectDelivery.js): асинхронное чтение Item отдельно от записи; конечный fallback другому GM. Местное право Actor → прежний owner/GM; узкие source/delivery Queries возвращают данные и никогда не пересылают.
- [effectDeliveryLocal.js](../../module/scripts/effectDeliveryLocal.js): обычные AE, семейства и выбранное улучшение Item через прежние методы; статусы ожидают присутствие/запись/снятие при иммунитете. Общий пакет Actor обёрнут в withParameterChanges: нет обрезания ресурсов в промежуточном состоянии.
- Обёртки AE/статусов возвращают complete/refused/unknown; неизвестный ответ не вызывает повтор. results подтверждает только выполненные строки. Исключение после начала локального пакета консервативно unknown, в том числе отмена существующего выбора оружия без отдельного признака фазы. Это не журнал транзакции.
- Два новых предупреждения через ru/en-ключи. Прежний общий query сохраняет whitelist и true после возврата функции: потребители нового протокола используют отдельные handlers. Объединение self/targets и чат-карточка — .007; дополнительные потребители — .008. До этих стадий не заявляется полное исправление00185.
- Существующий lookup statuscounter (issue-00003) не исправлен: при включённом модуле и ненулевом сроке исключение после создания статуса отражается как unknown; прежняя остановка до иммунитета возможна. Старый массовый listener тоже вне этой порции.

### Сценарии S06

**49/49 выполненных проверок прошли:** 34 новых сценария доставки и15 прежних регрессионных. В копии прежнего набора шесть сценариев с изменившимся контрактом/потребителями пропущены; они не входят в49. Для новых ветвей исполняется настоящий код системы; модели полей, применение/семейства AE, native подготовка Actor и итоговая нормализация — настоящие. БД, сеть/права, UI выбора, ChatMessage и toggleStatusEffect — контролируемые подмены. Холодный compendium моделируется различием sync/async lookup; настоящий HTTP/pack/reload требует B06.

| Область | Проверено |
| --- | --- |
| Источник | Cold async, глубокая копия system.changes, пустой триггер, неверный тип/UUID, один GM fallback, нет GM/сам GM/таймаут, никаких Actor-записей при чтении |
| Исполнитель | Местное право, активный player-owner, GM при offline-owner; отсутствие/потеря прав до записи; query не пересылает |
| Подтверждение | Ожидание Promise записи/ответа; malformed true/таймаут → unknown, без повтора; отказ до записи; подтверждённые ранние строки при последующей ошибке |
| Статусы | Empty/present/applied/immune, все цели, числовой dataset0, ненумерический срок, ожидаемое снятие; отдельный counter-дефект не маскируется успехом |
| AE | Реальные изменения параметров, coalescing семьи, переносимые семейства/резервный источник, duration0/absent, полный формат улучшения выбранного Item и ожидание сообщения; source не меняется |
| Ресурс | Общее применение нескольких записей не обрезает промежуточно; финальное снижение максимума сохраняет корректировку до возврата |

Артефакты: `work-cat/witcher-task0011-006-20260919/` вне системы. Команды:

```bash
node --experimental-vm-modules test-delivery.mjs
node --experimental-vm-modules --test application-regression.test.mjs
```

Синтаксис изменённых JS, две локали/отсутствие удалённых ключей, версия, ссылки, метаданные и граф проверяются адресно; итог команд — `final-check.json` в каталоге артефактов. Полный набор справочника не требуется. Foundry, мир, компедиумы и права этой порцией не изменяются; .007 — следующий шаг, игровые проверки по отдельной команде пользователя.

**Дополнение S06:** возврат native `true` без активного статуса не считается применением. Адресные6 методов справочника (доставка AE/статусов/регистрация queries и зависимые семейства) прошли; журнал `graph-tests.log`. Исторические утверждения этих методов обновлены по новому разделению файлов, остальные разделы не запускались.


<a id="task-0011007"></a>
## TASK-0011.007 — общий набор и карточка отправки

2026-09-19, **14.3.1.00107**, dev. Реализована .007; B07 впереди. Рабочие файлы проверок: `/var/lib/foundryvtt/Data/work-cat/witcher-task0011-007-20260919`.

**Изменение:** effectDelivery.js дополняет доставку .006 JSON-снимком группы и карточкой, chat.js связывает её кнопку. castSpell ожидает исходный update STA, сохраняет ChatMessage броска и передаёт все четыре канала воздействий одним набором. Добавлены HBS и подписи ru/en. Исходная карточка магии, EV, крит/урон/регионы и остальные потребители .008 не перерабатывались.

**Поведение:** ни одна цель, включая self, не получает эффект до общей проверки исполнителей. waiting допускает повтор этой проверки; затем sending записывается перед первым эффектом. complete запрещает повтор, неизвестный ответ/частичный отказ/прерванное sending требуют ручной проверки. Сохраняются результаты отдельных строк; после отказа следующие цели не отправляются. Повторные токены одного Actor не дублируют одну запись. Случайный срок, AE, имена/UUID и ссылка на бросок копируются; новая выделенная цель или правка источника их не заменяет.

**Видимость:** whisper/blind копируются из фактического ChatMessage броска. Native blind скрывает список даже от автора; hook оставляет ему нейтральные состояние/кнопку без раскрытия имён или эффектов. Иной пользователь не получает кнопку инициатора. WeakSet исключает повторную подписку, activeSends — местное двойное нажатие. Распределённой блокировки нет.

| Команда / журнал | Результат и охват |
| --- | --- |
| `node --experimental-vm-modules --test cards.test.mjs spell-regression.test.mjs` → grouped-tests.log | **65/65**:37 новых сценариев карточки/интеграции +28 прежних EV/компенсации |
| `node --experimental-vm-modules test-delivery.mjs` → delivery-regression.log | **34/34** регрессии доставки .006, локальные примитивы/семейства/нормализация |

S07 включает три цели с недоступной, повтор без изменений, появившегося GM, JSON/reload, сохранённые значения/duration0, отсутствие пустой карточки, успешные present/immune, потерянный ответ после фактической записи, частичную запись, отказ следующей цели, запрет повтора после complete/needsReview, отказ создания/сохранения сообщения, локальное двойное нажатие, повторный render, четыре режима видимости и blind controls. Реальный castSpell проверен на ожидание STA, отменённую запись до броска, fumble, единственное списание35→32 и сохранённую случайную длительность3 при ручной отправке; новый бросок при повторе не выполняется. Падение создания карточки сохраняет исходный Roll.

**Пределы:** исполнялись настоящие модули системы, native Foundry модели/Roll и Handlebars. Сетевые User.query, хранилище ChatMessage/Actor и DOM-контейнер заменены управляемыми doubles; Roll.toMessage/toAnchor заменяют границу UI/сервера, не формулу броска. Начальные два сбоя S07 относились к harness: native toAnchor требовал DOM, генератор тестового d6 был настроен для d10; исправлены фикстуры, затем65/65. Живая сеть, права HTTP, сохранение в БД и перезагрузка браузера не проверялись. Это не атомарная операция нескольких Actor и не обещание восстановления после потери ответа; автоматического отката/автоповтора нет.

Документация: обновлены task/очередь, ход issue-00185, карточки сервиса/castSpell/chat/ru/en/манифеста, добавлена карточка HBS и строка реестра (644 исходника). Справочник переводит текущий castSpell с прямых четырёх обёрток на collectSpellEffects/createEffectDelivery, отражает await STA и путь кнопки. Полная регрессия справочника не требуется; проверяются изменённые процессы/связи и целостность.

**Дальше:** .008 — остальные потребители и последствия боя; B07 — реальные QA-клиенты, видимость и повтор после reload по команде пользователя. Issues не закрываются, Foundry/мир/компедиумы и права доступа не менялись.

**Адресная сверка справочника:** семь методов по await STA, новому пути castSpell, сохранённой компенсации EV, регистрации chat hook, выбору до расхода, связи с outer parameter operation и группе/карточке прошли. Шесть прошли в `graph-tests.log`, седьмой — в `graph-chat-recheck.log` после замены устаревшего жёсткого номера строки hook на проверку его фактического вызова. Первый подготовительный запуск остановился до тестов из-за ещё не заполненного SHA нового шаблона; после обновления метаданных Dataset загружается. Новые записи:11 сущностей,38 отношений,1 процесс;16 исчезнувших прямых отношений зарезервированы как retired, существующие ID сохранены.

Синтаксис изменённых JS, JSON ru/en и новые24 ключа в каждом языке,114 добавленных документальных ссылок/якорей, `git diff --check`, неизменность прежних стадий вне .007 и mode/uid/gid/inode существовавших файлов проверены. Результат — `static-check.json`. Код .006 в прежних функциях сервиса и три старых обработчика chat.js сохранены побайтно.


<a id="task-0011008"></a>
## TASK-0011.008 — потребители и последствия боя

2026-09-19, **14.3.1.00108**, dev. Реализация/S08 выполнены локально, B08 ожидается. Артефакты: `/var/lib/foundryvtt/Data/work-cat/witcher-task0011-008-20260919`.

**Изменения и границы:** профессия передаёт уже вычисленный AE в карточку вместо прямого query владельцу. Попадание/урон/расходник используют createItemEffectDelivery: один async resolve источника → копия AE → карточка .007. При недоступном источнике — предупреждение с видом последствия, Actor/Item и исходным сообщением, не рекурсия. Уже рассчитанное основное действие не повторяется. Расходник публикует сообщение до AE, applyDamageFromMessage передаёт оригинал для видимости. Вход без исходного ChatMessage использует native текущий messageMode и не рисует пустую ссылку.

Парирование ожидает staggered на1 раунд исходному атакующему; отказ/unknown создаёт явную apply-status ссылку. Ручной клик требует права на указанный Actor, не использует другой выбранный токен, присутствующий статус не дублирует. WeakSet/местный lock ограничивают повторную подписку/одновременный клик. Старые ссылки без UUID сохраняют getCurrentCharacter. Адреналин: optional off/no-op, местное право первым, иначе имеющийся owner/GM query. addAdrenaline ожидает update и не принимает отменённую запись за успех. Предупреждение сначала требует проверки уже сохранённого результата и лишь затем ручного +1; крит и сообщение защиты сохраняются.

| Команда / журнал | Фактический результат |
| --- | --- |
| `node --experimental-vm-modules --test consumers.test.mjs cards.test.mjs` → combined-tests.log |75/75:38 сценариев потребителей и37 регрессионных карточки |
| `node --experimental-vm-modules test-delivery.mjs` → delivery-regression.log |34/34: регрессия API/query/примитивов/семейств/нормализации |
| `node --experimental-vm-modules --test consumers.test.mjs` → consumers-final.log |39/39 после добавления notification при отмене самого сообщения предупреждения; остальные37+34 остаются действительны, итог110 различных сценариев |

Проверены сохранённые триггеры/AE/duration0, пустые источники, неизвестный UUID, whisper/blind, ошибка создания карточки, автоматическое/ручное ошеломление, другой выделенный Actor, местное право/отказ/legacy ссылка, повторный render и три клика до завершения записи. Для адреналина — optional off, ожидание update, +1 один раз, нет исполнителя, серверная отмена, фактический старый dispatcher, потеря ответа. Настоящий skillDefense сохраняет один опубликованный критический бросок при отсутствии исполнителя, потерянном ответе и выключенной опции. Урон численно исполняется один раз при повторе карточки, consume не повторяет лечение/self/removes/сообщение; исходное сообщение остаётся при потере Item. Профессия сохраняет прежний готовый payload/срок и не перебрасывает способность; неуспех/нет цели не создают AE. Публичный API ViaId проверен на асинхронный источник и конечный JSON-результат. HBS ru/en экранирует имена и не выводит новые сырые ключи.

**Настоящие и заменённые части:** исполнялись текущие модули системы, native модели/Roll/семейства из стенда .006/.007 и настоящий Handlebars. БД/сеть/ChatMessage/DOM — управляемые doubles; в профессии конструктор источника AE фиксировал точный прежний вход, а исходный бросок способности задан заранее. В skillDefense исполнялся настоящий метод/extendedRoll с фиксированным исходом checkForCrit, чтобы проверить сохранность результата, не правила критов. Первые неуспешные запуски выявили неполноту фикстур (alias target, defaults whisper/blind, CONFIG.queries/CONST) и ожидание неэкранированного знака =; исправлен стенд. Старая legacy константа профессии выдаёт native deprecation — это сохранённый payload TASK-0012, не новая механика.

Не проверены живые клиенты, запись БД/HTTP и фактический Foundry UI. Независимые status/HP/износ/расходы/токсичность и отдельные не-await callers не объявляются объединёнными в транзакцию. API продолжает прямую конечную доставку без карточки, игровые потребители с контекстом действия используют карточку. Автоматического отката/повтора unknown, нового расчёта temporaryHP и блокировки хода нет.

Документация/реестр/граф обновлены по потребителям, await адреналина, явному Actor ссылки и новому HBS (645 исходников). Issues00045/00185 остаются в своих каталогах до общей приёмки. Следующая .009; Foundry/мир/компедиумы/права не менялись.

**Адресная сверка справочника:** семь методов по расходнику/профессии, явному статусу, общей доставке/семействам и потребителям прошли. Шесть — в `graph-tests.log`, седьмой — в `graph-status-recheck.log`: первый вызов ошибочно указал имя класса теста, после исправления имени сам тест прошёл без изменения кода. В графе добавлены6 сущностей и35 отношений,22 устаревших прямых отношения отмечены retired; существующие ID и границы охвата сохранены.

Проверены синтаксис изменённых JS, JSON ru/en,13 новых ключей в каждом языке без изменения прежних значений,133 добавленные документальные ссылки/якоря и `git diff --check`. Сопоставление с началом .008 подтвердило сохранность прежних стадий вне согласованных потребителей и mode/uid/gid/inode существующих файлов. Результат — `static-check.json`; заключительная целостность/актуальность справочника фиксируется отдельно в `index-check.json` и `final-check.json`.


<a id="task-0011009"></a>
## TASK-0011.009 — итоговая статическая приёмка

**2026-09-19, 14.3.1.00109.** `dev`, HEAD `3f0538de820dcd83265ef38d00adaa3002d1d650`, сохранённые незакоммиченные .002–.008. Изменения всей TASK-0011 сопоставлены с `7557e294d27102e59338e79a7796b81a299f6077` перед .001. Артефакты этого прогона: `/var/lib/foundryvtt/Data/work-cat/witcher-task0011-009-20260919/`. Исторические логи прошлых стадий не перезаписывались.

**Итог:** .009 выполнена; **315 различных успешных сценариев**,14 адресных проверок графа. Рабочий JS/HBS/локализации на этом этапе не менялись. В репозитории изменены итоговые документы, одно устаревшее ожидание теста графа, счётчик манифеста и зависимые хеши. Все11 issues остаются в прежнем статусе до B01–B09 и подтверждения пользователем.

### Единый прогон актуальных исходников

Во всех стендах читались текущие исходники системы. Выполнялся Node с `--experimental-vm-modules`; файлы `*.test.mjs` запускались через `--test`, `test-delivery.mjs` — напрямую. Полные команды сохранены в `behavioral-results.json`; воспроизведение исправленного набора — `python3 run-tests.py` из каталога артефактов .009.

| Набор | Сценарии / результат | Журнал .009 |
| --- | --- | --- |
| S01: восстановление/сердечки V2/V1, оба sidebar |36 прошли | resources.log |
| S02: четыре маршрута навыков и независимые листы |6 прошли | skills.log |
| S03: native вход меню, формы словесного боя, Resolve/Counterargue |38 прошли | verbal.log |
| S04: алхимия, повреждения/ремонт, стоимость, исполнитель и запись |38 прошли | repair.log |
| S05: контейнеры/шаблоны/экипировка, прежние UUID/вес/операции |42 прошли | containers.log |
| S05: переносимая семья AE, действующий HP/max и итоговая нормализация |2 прошли после подключения актуального стенда | effects-recheck.log |
| Прежняя регрессия применения/длительности/семейств |15 прошли,6 исторических сценариев исключены | application.log |
| Компенсация EV, произвольный/условный вклад и стоимость STA |28 прошли | spell.log |
| S07–S08: карточка/повтор и игровые потребители последствий |76 прошли:37+39 | consumers-cards.log |
| S06: источник, права, query/API, состояния результата, локальная запись |34 прошли | delivery.log |

**Корректировки проверок:** первый `containers.log` содержал42 успеха и2 ошибки загрузки: старый стенд TASK-0010 не предоставлял новые экспорты effectDelivery. В копии `effects-regression.test.mjs` подключён актуальный стенд .008, прежние ожидаемые значения сохранены; обе проверки прошли. Старые тесты/артефакты не изменены. `behavioral-results-initial.json` сохраняет первый запуск; итог и команды повтора — `acceptance-results.json`.

Шесть пропусков из прежнего application-набора (A06/A12/A13/A14/A17/A18) не включены в315. Их прежние требования о пробросе отказа основному действию, прямых запросах и порядке сообщения consume заменены согласованным контрактом .006–.008. Текущие сценарии отдельно проверяют отменённую запись/улучшение, сериализацию и duration0, конечный API/query, сохранение результата hit/damage и сообщение consume до отдельной карточки. Пропуски не объявляются успешными тестами старого поведения.

### Сопоставление с issues и границы

| Issues | Подтверждено статически | Остаток в .010 |
| --- | --- | --- |
|00164,00203 | Восстановление до действующего max; состояние HP по текущему max, запас/ресурс раздельно | B01: реальные кнопки, эффекты, сохранение/reload, оба листа |
|00167 | jQuery не перезаписывается, события идут своему Actor | B02: штатный редактор и повторный render |
|00302,00303 | Native меню и независимые формы, отсутствие выбора, сохранение прежнего расчёта | B03: клики меню/label, два окна и выбор Actor |
|00037,00102 | Getter алхимии, список повреждений, материалы/ремонт/await | B04: реальное создание предметов и GM/artisan |
|00277,00254 | Снятие экипировки при хранении, импорт/извлечение, подавление AE и ограничение суммарного EV | B05: drag-and-drop, интерфейс и запись документов |
|00045,00185 | Конечное чтение источника/исполнитель; ожидание всего набора; сохранённый результат без повторного броска/расхода; предупреждения | B06–B09: HTTP/«холодный» компедиум, два клиента/GM, reload, видимость, последствия боя |

Реальны: модули системы, соответствующие методы/модели, native Foundry модели и подготовка в стендах AE, Roll/extendedRoll и Handlebars. Где расчёт не был предметом сценария, результат броска задан фикстурой. Управляемые подмены: БД/сеть/User.query, коллекции и запись документов, DOM/окна/ChatMessage; полные границы по каждой стадии описаны выше. Формула временных ПЗ профессии не подтверждается как исправленная — она остаётся TASK-0012. Рефакторинг критов — TASK-0013. Опциональная старая интеграция statuscounter/issue-00003 не исправлялась этим набором.

Реальная сеть, серверное сохранение, права HTTP, браузерная вёрстка и фактическое истечение улучшений в игре ещё не проверены. [Принятое ограничение неизвестной доставки](../documentation/known-limitations.md#effect-delivery-uncertain) отражает согласованное поведение и синтетическое воспроизведение, а не пройденный игровой тест.

### Исходники, граф и документация

- Состав TASK-0011:35 игровых исходников, включая27 JS,6 HBS и2 словаря, плюс system.json. Перечень/хеши — `source-check.json`. Синтаксис27 JS прошёл;6 изменённых HBS отрендерены в соответствующих наборах выше. Новые43 ru/en-ключа совпадают, параметры подстановки совпадают, старые ключи/значения сохранены. Проверены21 конкретный ключ новых потребителей и динамические группы состояний/причин/действий доставки.
- 14 адресных тестов: ресурсы/sidebar/recovery, подписки навыков, chat/repair/query, consumers→prepareCheck, выбор до расхода, границы записи Item/AE, finally, группа/карточка/потребители/семейства.13 прошли в `graph-tests.log`; тест ремонта сначала обнаружил старый номер строки. Он теперь читает диапазон onRepairRequest из графа; повтор прошёл в `graph-repair-final.log`. Дополнительный ошибочный запуск из корня не импортировал тестовый модуль и не считается выполненным тестом.
- Повторно сверены реальные вызовы source/executor/query, все входы карточки, конечные обёртки публичного API, сохранение основного результата и границы документации. Граф используется с прежним partial-охватом; семантических новых узлов/отношений в .009 нет. Обновлена карточка манифеста, очередь/навигация/журнал и принятые ограничения. В таблице реестра задач исправлена устаревшая запись «следующая .007».
- Заключительная проверка ссылок, diff, сохранности исходников и mode/uid/gid/inode — `documentation-check.json`; один `query.py check --freshness --format json` после обновления хешей — `index-check.json`; сводка — `final-check.json`.

**Перед .010:** пользователь запускает/перезапускает Foundry и проверяет права изменённых/новых файлов. Агент не меняет права, службу, мир, packs и настройки компедиумов. После отдельной команды проводится согласованный QA-прогон B01–B09 и только затем готовится перечень issues к закрытию. Синтетическая приёмка не заменяет эту стадию.


<a id="browser-fixes-00114"></a>
## После браузерной приёмки: no-op установлен, количество подготовлено — .00114

2026-09-19, ветка dev. Пользователь поручил реализацию двух правок. Артефакты: `/var/lib/foundryvtt/Data/work-cat/witcher-task0011-quantity-20260919/`; `source/` — подготовленные исходники, `runtime.patch` — исходный патч 21 файла, `final-tests.tap` — итоговый общий запуск. В рабочий репозиторий установлен **только effectDelivery.js**, побайтно совпадающий с проверенной копией. Остальные 20 файлов ожидают проверки данных перед сменой схемы.

| Набор | Результат | Где применено |
| --- | --- | --- |
| Карточка/доставка, новые no-op сценарии, регрессия заклинания | 75/75 | Установленный effectDelivery.js |
| Модель/форма/экспорт/генератор количества | 40/40 | Подготовленная копия |
| Ремонт/алхимия, включая реальный BaseItem со строкой `'6'` → числом6 | 39/39 | Подготовленная копия; repair.js не меняется |
| Контейнеры и сохранение двух полей при копировании | 28/28 | Подготовленная копия |
| Синтаксис и локализация | 7 JS, 11 HBS, 4 новых ключа в каждой из ru/en | Подготовленная копия |

Команда: `node --experimental-vm-modules --test --test-reporter=tap tests/quantity.test.mjs tests/repair/repair-alchemy.test.mjs tests/containers/{container-regression,quantity-copy}.test.mjs tests/cards/{cards,noop,spell-regression}.test.mjs` из каталога артефактов. **182/182, 0 пропусков.** Проверены 5/3→1, 6/3→2, независимые броски до суммирования, 1/3→0, неверная формула до создания loot, отрицательный/неконечный результат до записи, порядок стопок/генераторов, настоящий отказ записи и no-op обоих полей. Контейнер сохраняет количество/формулу, выявляет изменённую при записи копию. Карточка сравнивает весь снимок, сохраняет различия даже при прежнем state; undefined настоящего update не принят за успех.

**Границы:** Foundry14.367 поля/DataModel/BaseItem и валидатор Roll настоящие; БД, Actor.create/update, UI, query и результаты случайных бросков подменены. Нативная очистка пустого raw JSON и null отличается от ручного ввода и отражена отдельным тестом; форма отвергает пустое/дробное/отрицательное значение. Служба Foundry остановлена, игровые проверки не запускались. В копиях старых стендов обновлены строковые quantity, источник кода и модель no-op; сравнение cross-realm JSON предварительно переносит оба объекта в один realm и вызывает настоящий equals. Первые неуспешные логи сохранены: ошибки подмен/путей исправлены, старые стенды не переписаны.

**Препятствие установке количества:** чтение `worlds/witcher-rb-test/world.json` и актуальных файлов `data/actors`, `data/items`, `data/scenes` запрещено правами; в частности CURRENT и новые .log/.ldb. Согласованный проект требует проверить сохранённые формулы/дроби **до** переключения схемы. Пользователю отправлен запрос доступа для чтения, права/владельцы и данные не менялись. Нельзя считать старый диагностический снимок проверкой актуальной БД. После получения доступа прочитать копию БД/доступные источники, согласовать адресное исправление несовместимых записей при их наличии, установить оставшийся патч, обновить документацию/граф количества; затем B04/B07 по команде пользователя после перезапуска. Новая схема, системные packs, браузерная приёмка и закрытие issues этим результатом не объявляются выполненными.


<a id="quantity-installed-00115"></a>
## Установка количества после восстановления доступа — 2026-09-19, 14.3.1.00115

**Установлено:** оставшиеся20 файлов согласованного проекта .004. Quantity — конечное целое ≥0 с initial1; формула добычи вынесена в lootQuantityFormula. Пустой/нечисловой/отрицательный/дробный ручной ввод отклоняется. Формула проверяется Roll.validate до экспорта; каждый результат округляется вниз до сложения:5/3→1,6/3→2, два5/3→2. При пустой формуле используется quantity×multiplier. Экспорт очищает формулу у полученной добычи, исходный шаблон сохраняет. Обновлены общая форма, активные инвентари, контейнерное подтверждение записи и ru/en. Repair и containerTemplates не потребовали изменения исходников.

### Проверка данных до установки

Foundry остановлена. Для чтения скопированы только actors/items/scenes из worlds/witcher-rb-test/data; ClassicLevel открывал копии, исходные БД не открывались для записи. Прочитаны2 Actor,0 Item и8 записей БД scenes (включая служебные/вложенные записи; это не утверждение о восьми сценах). Оба персонажа без Items; токенов, синтетических Actor, templateContent и ссылок на источники предметов мира нет. Отсутствуют quantity и несовместимые значения. В226 JSON системных packs также нет quantity. Перенос данных и пересборка системных packs не нужны. Чужие миры и все сторонние content-packs сервера не проверялись.

### Адресные результаты

Рабочие артефакты: `/var/lib/foundryvtt/Data/work-cat/witcher-task0011-quantity-20260919/audit-00115`.

- `check-data.mjs`, `quantities.json`: проверка сырых записей мира, вложенных деревьев и packs;0 несовместимостей.
- `installed-tests/quantity.test.mjs`:40 сценариев настоящих Foundry fields/TypeDataModel/BaseItem и Roll.validate, форм, экспорта и таблиц.
- `installed-tests/repair/repair-alchemy.test.mjs`:39 сценариев ремонта/алхимии, включая настоящую модель Item с входной строкой "6" и расход6→5.
- `installed-tests/containers/{container-regression,quantity-copy}.test.mjs`:28 сценариев контейнеров и копирования количества/формулы.
- `installed-tests.tap`: **107/107 успешно,0 пропущено**; модули импортируются из установленного репозитория, не подготовленной копии.
- Прежние75 проверок исправления карточки/доставки/заклинания .00114 сохраняют силу: effectDelivery.js в этой установке не менялся.
- Синтаксис7 JS-файлов, компиляция11 изменённых HBS и4 новых ключа×ru/en проверены успешно (`syntax.json`).

Команда запуска из каталога артефактов:

```bash
node --experimental-vm-modules --test --test-reporter=tap installed-tests/quantity.test.mjs installed-tests/repair/repair-alchemy.test.mjs installed-tests/containers/container-regression.test.mjs installed-tests/containers/quantity-copy.test.mjs
```

Границы: DB/пользовательский клиент и результаты случайных бросков в сценариях подменены; это не игровой прогон. Настоящий NumberField ещё выполняет штатное приведение raw JSON/API: пустая строка становится0, null — initial1. Формы отдельно отвергают пустой ввод; ItemQuantityField сохраняет отрицательные/дробные числа для отказа валидации вместо незаметного clamp/round. Совместимость всех произвольных старых JSON не обещается.

### Документация и повторная приёмка

Целостность/актуальность справочника: `query.py check --freshness --format json` — valid/current,0 расхождений. Четыре адресных CLI-сценария подтвердили читателей/писателей quantity и lootQuantityFormula и вызывающие обе функции проверки. Синхронизированы ожидания числового quantity в существующих тестах справочника; исторические тестовые пакеты целиком не запускались. Ссылки изменённых документов проверены; `git diff --check` без ошибок. Исходные27 файлов БД мира и метаданные существующих файлов репозитория (mode/uid/gid/inode) сохранены. Артефакты: `index-check.json`, `index-cli.json`, `final-verification.json`.

Обновлены задача, issue-00102, DEC-0007, навигация, changelog, карточки затронутых файлов и ремонта; граф содержит числовой контракт, отдельное поле формулы и зависимости его проверки/экспорта/копирования. Исторические результаты .00109/.00114 сохранены отдельно от текущего состояния.

После запуска Foundry по команде пользователя: повтор B04 (обычный ремонт, недостаток материалов, artisan/GM), сохранение/повторное открытие количества и формулы, экспорт фиксированной/формульной добычи и RollTable, перенос наполненного контейнера; B07 — неизменившееся waiting, возвращение исполнителя и настоящий отказ записи. Полный повтор остальных игровых сценариев без новых оснований не нужен. TASK-0011/.004/.007/.010 и issues пока не закрываются.

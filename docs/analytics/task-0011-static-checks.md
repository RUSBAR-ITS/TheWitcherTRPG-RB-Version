# TASK-0011 — Статические проверки исправлений

<a id="task-0011001"></a>
## TASK-0011.001 — восстановление Выносливости и индикатор HP

**2026-09-19, 14.3.1.00101.** Ветка `dev`, база `7557e294d27102e59338e79a7796b81a299f6077` плюс описанные правки. [Подзадача](../tasks/task-0011-modifier-integration-followups.md#task-0011001) реализована и проверена локально; игровая приёмка B01 ещё не выполнена. Остальные подзадачи .002–.010 этим отчётом не объявляются выполненными.

### Реализация

- В [WitcherActorSheet](../../module/actor/sheets/WitcherActorSheet.js) и [WitcherActorSheetV1](../../module/actor/sheets/WitcherActorSheetV1.js) Recovery Action пишет `min(sta.max, sta.value + rec.value)`. Значения берутся при подтверждении; guard полного запаса, уведомление, Full Recovery и ожидание Actor.update сохранены. Регистрация V1 не менялась.
- В [sidebar персонажа](../../templates/sheets/actor/partials/character/sidebar.hbs) и [sidebar монстра](../../templates/sheets/actor/partials/monster/sidebar.hbs) целое сердце определяется по действующему `hp.max`. Операция сравнения `>=`, порог ранения и цвета сохранены. Максимумы, текущие ресурсы и temporaryHpSum шаблонами не меняются.
- Новых интерфейсных подписей нет; существующая локализация сохранена. Формулы производных параметров, схема ресурса, компедиумы и права файлов не менялись.

### Поведенческие проверки

**36/36 прошли**, пропусков нет. На исходном коде .00100 эти же сценарии дали **26 успешных и10 неуспешных**: шесть проверок переполнения REC и четыре проверки сердечка при изменённом максимуме. После исправления все36 проходят. Синтаксис обоих JS проверен `node --check`.

| Область | Проверено |
| --- | --- |
| Recovery V2/V1 | 9/10+3→10; 4/10+3→7; 0/10+3→3; 2/5+8→5 |
| Полный/превышенный запас | Для обеих кнопок при10/10 и12/10 нет записи, есть прежнее уведомление; текущий избыточный запас не срезается кнопкой восстановления |
| Данные на момент подтверждения | Изменённые после открытия value/max учитываются; Full Recovery использует актуальный max |
| Закрытие/ожидание | Открытие без выбора не пишет данные; оба callback ожидают Promise Actor.update и передают отказ записи наверх |
| Обе панели HP | 40/60 при исходном40 — разбитое зелёное; 35/35 при исходном40 — целое зелёное; 5/40 при threshold10 — оранжевое; ровно threshold — разбитое зелёное; 45/40 — целое |
| Отображение и данные | Весь HBS рендерится, число max правильное, входные данные не мутируют, временные ПЗ не подменяют заполненность постоянного запаса |

**Методика и пределы:** импортированы настоящие ES-модули обоих листов через vm.SourceTextModule; исполняются их реальные `_onRecoverSta` и callbacks. Базовые классы Foundry, неиспользуемые примеси, DialogV2, локализация/уведомления и Actor.update подменены. Полные реальные HBS обработаны установленным Handlebars с фасадами стандартных helpers. Запись БД, сеть, native окно и реальная регистрация обработчиков этим не подтверждены.

Артефакты: `/var/lib/foundryvtt/Data/work-cat/witcher-task0011-001-20260919/`: `resources.test.mjs`, `before-tests.log`, `after-tests.log`, baseline с хешами/метаданными. Запуск:

```bash
node --experimental-vm-modules --test /var/lib/foundryvtt/Data/work-cat/witcher-task0011-001-20260919/resources.test.mjs
```

### Адресная сверка справочника

Обновлены две сущности wound-state, их чтение `stat().max` вместо `stat().unmodifiedMax`, REC callbacks и процессы ожидания записи. Уточнены сохранившиеся исторические формулировки «без ожидания» в Full Recovery: await уже был в коде до этой порции. После добавления двух строк в каждый JS перенесены адреса зависимых записей; новые сущности и процессы не добавлены, IDs сохранены.

Прошли **3 адресных теста** `ResourceFormExpansion` из `test_expansion_024.py`: `test_source_prepared_maximum_and_display_remain_distinct`, `test_recovery_guards_cap_and_awaited_update`, `test_sidebar_control_contracts_and_reverse_writers`. Первый теперь проверяет текущий контракт sidebar и его чтения; устаревшие строковые ожидания старого вычислителя из TASK-0010 не используются как проверка этой UI-правки. Поведенческая проверка формулы — отдельные36 сценариев выше, не поиск строки Math.min.

Команда из `docs/analytics/system-index/tests`:

```bash
python3 -B -m unittest test_expansion_024.ResourceFormExpansion.test_source_prepared_maximum_and_display_remain_distinct test_expansion_024.ResourceFormExpansion.test_recovery_guards_cap_and_awaited_update test_expansion_024.ResourceFormExpansion.test_sidebar_control_contracts_and_reverse_writers -v
```

Полный набор справочника не запускался. Итоговая проверка ссылок/метаданных и один `query.py check --freshness --format json` выполняются после обновления документов и хешей; протокол — `final-check.json` в каталоге артефактов.

### Что осталось

Игровая B01 по команде пользователя: штатная кнопка восстановления, эффект изменения max и reload; сердечко персонажа и монстра. Issues00164 и00203 сохраняют статус potential до дальнейшего решения пользователя; они не закрываются по синтетическим результатам. Следующая порция реализации — TASK-0011.002.

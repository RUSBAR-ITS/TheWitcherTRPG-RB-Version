# TASK-0010.002 — Контракты эффектов и тесты перед исправлением

Дата: 2026-09-18. Ветка `dev`, HEAD `0537500936de736ce898b0173670c82714b72f23`; исходники до реализации TASK-0010, версия оформления результата **14.3.1.00060**. Foundry **14.367.0**, Node **24.16.0**. Основание — [согласованный проект](../tasks/task-0010-parameter-limits.md#task-0010002) и поручение начать исправления.

**.002 выполнена как подготовка запускаемых тестов и проверка границ. Исправления игрового кода ещё не выполнены.** Красные проверки ниже показывают расхождение текущей системы с принятым результатом; это не успешная приёмка исправлений. Статусы issues этим протоколом не меняются.

## Артефакты и запуск

Рабочий каталог: `/var/lib/foundryvtt/Data/work-cat/witcher-task0010-002-20260918/`. Как на предыдущих этапах, тестовый код расположен вне поставки системы; зависимости и `package.json` системы не менялись.

```bash
cd /var/lib/foundryvtt/Data/work-cat/witcher-task0010-002-20260918
python3 run.py
```

Общий запуск сейчас завершится **кодом 1**, поскольку тесты ожидают уже исправленного поведения. Результаты сохраняются в `results.json` и трёх `*.tap`; в JSON записаны версии, HEAD, отдельные результаты и хеши проверяемых входов. Код запуска не превращает ожидаемое падение в успешную проверку.

| Набор | Выполнено | Результат | Что означает |
| --- | --- | --- | --- |
| [native.test.mjs](/var/lib/foundryvtt/Data/work-cat/witcher-task0010-002-20260918/native.test.mjs) | 13 | 13 проходят | Фактические контракты ядра/моделей и существующего подавления |
| [regressions.test.mjs](/var/lib/foundryvtt/Data/work-cat/witcher-task0010-002-20260918/regressions.test.mjs) | 45 | 6 проходят, 39 падают | Желаемое поведение моделей, редакторов, расчётов, бросков и доставки; все 25 issues имеют адресный сценарий |
| [calculation.test.mjs](/var/lib/foundryvtt/Data/work-cat/witcher-task0010-002-20260918/calculation.test.mjs) | 0 из 32 | 32 явно пропущены | Независимые числовые примеры для ещё не созданных вычислителя и сохраняемых операций |

Пропущенные проверки не подтверждают арифметику. В `.004–.006` `calculation-adapters.mjs` должен быть подключён к настоящим функциям системы: разрешены перевод формы входа и фикстуры документов, **запрещена реализация расчёта внутри адаптера**. При изменении внутренних методов старые тесты перенаправляются к новым входам с сохранением принятого ожидаемого результата.

`pack-change-inventory.json` — отдельная инвентаризация чтением JSON: **358 строк изменений**, найденных в эффектах документов `packsJson`. Она не заменяет будущую матрицу травм, проверку установленных баз или соответствия правилам. Данные компедиумов не редактировались и не собирались.

## Границы достоверности

- Настоящие установленные `DataModel`, `TypeDataModel`, `ActiveEffectTypeDataModel`, `BaseActiveEffect`, поля, сериализация, Roll/грамматика и Handlebars. Схемы системы импортируются из текущих файлов.
- Методы Actor, ActiveEffect, редактора, формул, ремонта и доставки загружаются из исходных JS через VM; реализации тестируемых методов не переписаны. Их оболочки документов, несвязанные импорты, настройки, DOM, окна и запись заменены фиксированными зависимостями/регистраторами вызовов. `harness.mjs` явно перечисляет загружаемые файлы.
- N12 исполняет настоящий `Actor.applyActiveEffects` и `allApplicableEffects`; сам обработчик применения одной строки, hooks и объединение результата между VM-контекстами подменены. Операции числового поля независимо проверены N05/N06. Этот сценарий не доказывает целиком клиентский `prepareData`.
- В бросках подменены только источник случайных чисел и внешнее окно исполнения кубов; арифметика/парсер/`extendedRoll` настоящие. Два сценария перехватывают `Roll.toMessage`; сетевое создание сообщения не выполняется.
- R29 создаёт реальные системные модели Attack/Damage и повторно читает их после JSON-сериализации. Это проверка схемы payload, **не запись ChatMessage в БД**. R26 использует настоящую schema копии AE, но не клиентский lifecycle создания документа.
- Подмена журнала прокачки фиксирует отдельные вызовы `addIpReward`; параллельная запись в БД не воспроизводилась. Существующая реализация [logData](../../module/data/actor/templates/character/logData.js) проверена чтением как отдельный сохраняющий путь.
- Foundry/мир/браузер не запускались, HTTP-доступ службы и права не проверялись. Reload, два клиента, настоящая форма, init/start и истечение эффектов остаются игровой приёмкой `.013`.

При первичном запуске исправлены только недостающие части тестового окружения (окно исполнения кубов, default-конфигурация защиты, граница VM) и две гипотезы о поведении ядра. Ядро сохраняет не перечисляемый псевдоним `changes`, а округление integer-поля — `Math.round`, не `floor`. Принятые игровые ожидания под текущие дефекты не изменялись.

## Проверенные контракты Foundry 14

| Граница | Фактический контракт | Последствие для реализации |
| --- | --- | --- |
| Строка изменения | `key`, `type`, `value`, `phase`, `priority` в `system.changes`; N01–N04 | Расширять schema строки, сохранять тип/фазу/приоритет, не вводить параллельный список бонусов |
| Старый root `changes` | Ядро мигрирует вход; в объекте оставляет неперечисляемый псевдоним, JSON хранит `system.changes`; N02 | Новый код пишет современный путь; существование псевдонима не означает отдельного хранилища |
| `_source` и prepared | Изменение `effect.system.changes[0]` не изменяет сериализованный источник; `updateSource` изменяет; N03 | Не считать изменение подготовленного объекта сохранением настроек |
| Типы операций | Schema допускает `add/multiply/override/upgrade/downgrade` и синтаксис пользовательских типов; N04/N05 | Допустимый синтаксис типа не доказывает наличие обработчика. Специальные типы нельзя автоматически трактовать как ADD |
| Порядок арифметики | Native-поле исполняет текущую операцию; последовательности дают `8×0.5+2=6` и `(8+2)×0.5=5`; N05 | Согласованный порядок должен обеспечивать общий вычислитель |
| Integer-поля | Для `7×0.5` native integer-поле выдаёт4; N06 | Не применять каждый промежуточный шаг к integer-полю, если требуется floor только в конце |
| Фазы Actor | `initial` в `prepareEmbeddedDocuments`, `final` после базового `prepareData`; один проход каждой фазы; N12 и чтение `/opt/foundryvtt/client/documents/actor.mjs` | Выделенные числовые строки должны обрабатываться ровно один раз, без повторного вызова уже завершённой фазы |
| Передача и отбор | Собственные AE + `transfer` на Item; inactive не применяется, token-строки отделяются; N12 | Не потерять источник, условия активности, статусы и изменения Token |
| Подавление | Неактивный/снятый предмет и флаги применения при использовании подавляют постоянное действие; N13 | Расчёт должен использовать фактически применимые эффекты |
| Временное улучшение Item | Отдельная модель наследует native changes, содержит `isTransferred`, не наследует WitcherActiveEffectData; N07 | Не подключать её случайно к расчёту характеристик Actor и не терять содержимое system при копировании |
| Длительность | `duration.value/units/expiry`, отдельный `start`; N08 | Присваивание legacy `duration.rounds` подготовленному источнику не сохраняет срок создаваемой копии |
| Бросок | Настоящий Roll принимает подписанные слагаемые; обычная атака `>` и защита `>=`; default текущего RollConfig — отсутствие оценки; N10/N11 | Форматирование не должно менять число; замену маркера отсутствующего порога выполнять вместе с его потребителями |

## Карта целей адаптера

Это границы будущего подключения, не уже написанный адаптер. Реестры/схемы и мастер проверены в [проекте](../tasks/task-0010-parameter-limits.md#files); текущие пути различаются по смыслу.

| Текущие адреса | Источник / потребитель | Обработка |
| --- | --- | --- |
| `system.stats.<key>.unmodifiedMax/max/value/totalModifiers` | statData, commonActorData, witcherActor, редактор | Сохранённая база отдельно от результата/суммы. Не считать `max` новым `baseCap`; для основных характеристик минимум1/потолок, для токсичности другая область |
| `system.derivedStats.<key>.*` | derivedStatsData, witcherActor | Формула либо разрешённая ручная база; собственные изменения производной после входов; запас HP/STA отдельно от максимума |
| `system.skills.<attribute>.<key>.value/activeEffectModifiers` | Skill, skillMixin, контексты атак | Адрес встроенного навыка, исходный уровень отдельно от эффекта/цены. Эффективный навык может быть отрицательным |
| `system.value`, `activeEffectModifiers` и `modifiers` у Item-навыка | собственная модель Item, rollCustomSkillCheck | Item ID; не поиск по `Item.name` среди встроенных навыков |
| `definingSkill/skillPathN.skillM` профессии | professionMixin, редактор профессии | ID Item + путь слота; одинаковые названия не меняют адрес |
| combat attack/defense, группы, lifepath | modifierMixin, defenseMixin, weaponAttackMixin, мастер AE | Численный вклад и подпись отдельно; `attacks.<strike>.value`, не весь объект |
| `combatEffects.turnStartEffects.*` | периодика/статусы, травмы | Объекты кровотечения/яда/удушья/кислоты не переводить целиком в арифметику параметров. Вложенный числовой `bleed.damage.modifier` сохранить по действующему контракту |
| `token.*`, строковые/объектные/пользовательские изменения, улучшение Item | native AE и специальные системные обработчики | Оставить соответствующий native/специальный путь; флаги численного параметра не распространять автоматически |

Таблица целей должна дополняться фактическими ссылками на новый резолвер в `.003–.004`. Полный граф и пофайловое описание существующей системы не заменяются этим сокращённым перечнем.

## Адресная связка всех 25 issues

Результат — наблюдение текущего кода в изолированном сценарии. Номера R относятся к `regressions.test.mjs`; «падает» означает нарушение желаемого контракта, а не новую зарегистрированную проблему.

| Issue | Вход → метод → результат/потребитель | Проверка и факт | Исправление |
| --- | --- | --- | --- |
| [00012](../issues/closed/issue-00012.md) | prepare → [calculateStats](../../module/actor/witcherActor.js) → luck/toxicity.max → лист/ресурс | R03: после двух проходов12/120 вместо10/110 | .004 |
| [00035](../issues/closed/issue-00035.md) | нагрузка → calculateStat → REF/DEX/SPD.value → броски | R04:4/4/6 вместо6/6/6 | .004 |
| [00036](../issues/closed/issue-00036.md) | эффект BODY.max → calculateStats → BODY.value/производные | R05: prepared max2 превращается в value8; R06: HP30 вместо32; R07: Leap3 вместо4 | .004–.005; настройка сердца/рёбер .010 |
| [00043](../issues/closed/issue-00043.md) | частичный submit → [AE._preUpdate](../../module/activeEffect/witcherActiveEffect.js) → phase | R08: forEach отсутствующего массива; R09: final сбрасывается в initial | .003 |
| [00051](../issues/closed/issue-00051.md) | переносимый AE на Item → [autocomplete](../../module/activeEffect/WitcherActiveEffectSheet.js) → варианты ключей | R30: подсказки Item вместо Actor | .003 |
| [00052](../issues/closed/issue-00052.md) | мастер → wizardAction → changes/update | R10: массив документа мутируется до успешной записи; несохранённая форма отдельно проверяется в .003 | .003 |
| [00194](../issues/closed/issue-00194.md) | подготовленный Actor → [stats-block](../../templates/sheets/actor/configuration/app/partials/stats-block.hbs) → submit базы | R11: value10 отправляется в поле базы8 | .003/.006 |
| [00195](../issues/closed/issue-00195.md) | автоматическая производная → тот же редактор → unmodifiedMax | R12: автоматическая база представлена обычным отправляемым input | .003/.005/.006 |
| [00017](../issues/closed/issue-00017.md) | покупка магического навыка → [levelUpSkill](../../module/actor/mixins/skillMixin.js) → IP update | R13: magicIP остаются3 вместо0 при цене8 и остатках3/7 | .006 |
| [00028](../issues/potential/issue-00028.md) | покупка → levelUpSkill/addIpReward → отдельные записи | R14: log и Actor вызываются отдельно. Только путь покупки, не награды/валюта | .006, частично |
| [00191](../issues/closed/issue-00191.md) | нехватка ОУ → levelUpSkill → уровень/балансы/лог | R15/R16: вместо отказа происходят записи, в том числе при суммарно недостаточных magic+обычных ОУ | .006 |
| [00192](../issues/closed/issue-00192.md) | прямой вызов повышения монстра → levelUpSkill | R17: обращение к отсутствующему logs; требуется безопасный запрет и ручной редактор | .006 |
| [00019](../issues/closed/issue-00019.md) | [мастер](../../module/activeEffect/mixins/baseMixin.js) / strong strike → [handleStrikeType](../../module/actor/mixins/weaponAttackMixin.js) → Roll | R18: объект попадает в формулу; R35: мастер выдаёт адрес объекта без `.value` | .008 |
| [00033](../issues/closed/issue-00033.md) | боевой бонус → [modifierMixin](../../module/actor/mixins/modifierMixin.js)/[defenseMixin](../../module/actor/mixins/defenseMixin.js) → Roll | R19Attack/Defense и R34: положительный вклад без знака не разбирается; проверен реально назначенный Actor-метод защиты | .007–.008 |
| [00204](../issues/closed/issue-00204.md) | AE-сумма без найденного имени → addActiveEffects → Roll | R20: формируется `+2[]` | .007 |
| [00187](../issues/closed/issue-00187.md) | Item-навык → [custom-skill-display](../../templates/partials/character/custom-skill-display.hbs) → UI-клик | R31: пустая подпись/уровень и действие встроенного навыка; настоящий клик остаётся браузерным | .008 |
| [00190](../issues/closed/issue-00190.md) | Item ID → rollCustomSkillCheck → addActiveEffects по имени → Roll | R21:12 вместо15, собственный AE+2 и общий+1 пропущены | .007–.008 |
| [00110](../issues/closed/issue-00110.md) | клик второго одноимённого слота → [professionMixin](../../module/actor/mixins/professionMixin.js) → поиск по имени | R32: выбирается definingSkill уровня1 вместо указанного слота уровня4; редактор проверяется дополнительно в .008 | .007–.008 |
| [00237](../issues/closed/issue-00237.md) | прямая атака профессии → doProfessionAttackRoll → extendedRoll | R33:14 вместо16; отсутствует корректная адресация бонуса и attack−2. Оружейная замена отдельно остаётся .008 | .008 |
| [00103](../issues/closed/issue-00103.md) | ремонт → [prepareRollFormula](../../module/item/systems/repair.js) → Roll | R22: чтение незарегистрированной настройки; снятие проверки после появления асинхронного сборщика запрещено | .008 |
| [00196](../issues/closed/issue-00196.md) | число порога → [RollConfig](../../module/scripts/rollConfig.js)/[extendedRoll](../../module/scripts/rolls/extendedRoll.js) → outcome | R23: оба−1 дают отсутствие outcome; шесть вариантов0/1/2 с результатом1 проходят. R24: legacy провал до0 ошибочно проходит невозможные `<=0` и `<1` | .007–.008 |
| [00042](../issues/closed/issue-00042.md) | улучшение → [applyTemporaryItemImprovements](../../module/actor/mixins/temporaryEffectMixin.js) → Item.createEmbeddedDocuments | R25: у копии отсутствуют changes | .009 |
| [00044](../issues/closed/issue-00044.md) | заданный срок → [applyActiveEffectToActor](../../module/scripts/temporaryEffects/applyActiveEffect.js) → clone / owner.query | R26:3 вместо9 в schema копии; R27: отсутствует срок в owner query | .009 |
| [00050](../issues/closed/issue-00050.md) | перенесённый AE на Item → _preCreate → combatant/start | R28: поиск combatant получает Item вместо его Actor. Native назначение start/времени и реальное истечение не проверены этим фасадом | .009/.013 |
| [00257](../issues/closed/issue-00257.md) | duration9 → [AttackMessageData](../../module/data/chatMessage/attackMessageData.js)/[DamageMessageData](../../module/data/chatMessage/damageMessageData.js) → JSON → schema | R29attack/damage: поле отсутствует у восстановленной модели | .009 |

R01/R02 дополнительно падают на отсутствии новых свойств и валидации. Это ожидаемая незавершённая функциональность `.003`. Проверки legacy критической ветви R24 — техническая защита автопровала00196, **не решение распространить криты на испытания**; рефакторинг критов остаётся [TASK-0013](../tasks/task-0013-critical-rolls.md).

## Следующий шаг и остаток проверки

Следующая подзадача — **.003, schema и редактор**. Сначала сделать зелёными R01/R02/R08–R12/R30 и добавить сохранение несохранённой формы/группы/соседних строк настоящим submit. Затем `.004–.009` исправляют свои красные сценарии и подключают числовые адаптеры. Нельзя объявлять проблему решённой только потому, что один из её текущих входов перестал использоваться: необходимо проверить заменивший его вход и зависимых потребителей.

Остаются дополнительные сценарии согласованной матрицы: транзитивные исключения, сердце/рёбра/customStat, сохранение обрезанной базы, условный выбор в окне, повтор семейства/длительность, установленный pack и реальная запись/клиенты. Их приёмка следует подзадачам; этот протокол не отмечает их выполненными. Готовность к запуску Foundry для игровых тестов пока не объявляется.

## Проверка оформления

Проверены926 локальных ссылок/якорей в изменённых документах, соответствие карты всем25 issues и сохранность метаданных существующих файлов. `git diff --check` — без ошибок. Один запуск `query.py check --freshness --format json` подтвердил целостность/актуальность справочника:624 источника,5648 сущностей,15693 связей,471 процесс; расхождений нет. Полный набор сценариев справочника не запускался: структура графа и игровой код не менялись, обновлён только источник манифеста/его карточка. Прежние пользовательские изменения и статусы259 potential / 0 open / 75 closed сохранены.
